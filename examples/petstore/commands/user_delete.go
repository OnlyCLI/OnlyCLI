// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/petstore/petstore-cli/runtime"
	"github.com/spf13/cobra"
)

var userDeleteCmd = &cobra.Command{
	Use:   "delete",
	Short: "Delete user resource.",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/user/{username}"
		usernameVal, _ := cmd.Flags().GetString("username")
		if usernameVal == "" {
			return fmt.Errorf("required flag --%s not set", "username")
		}
		path = strings.Replace(path, "{username}", usernameVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("DELETE", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	userDeleteCmd.Flags().String("username", "", "The name that needs to be deleted")
	_ = userDeleteCmd.MarkFlagRequired("username")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/petstore/petstore-cli/runtime"
	"github.com/spf13/cobra"
)

var petDeleteCmd = &cobra.Command{
	Use:   "delete",
	Short: "Deletes a pet.",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/pet/{petId}"
		petIdVal, _ := cmd.Flags().GetString("pet-id")
		if petIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "pet-id")
		}
		path = strings.Replace(path, "{petId}", petIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("DELETE", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	petDeleteCmd.Flags().String("api-key", "", "")
	petDeleteCmd.Flags().String("pet-id", "", "Pet id to delete")
	_ = petDeleteCmd.MarkFlagRequired("pet-id")
}

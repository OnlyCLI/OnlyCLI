// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"github.com/petstore/petstore-cli/runtime"
	"github.com/spf13/cobra"
)

var userLoginCmd = &cobra.Command{
	Use:   "login",
	Short: "Logs user into the system.",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/user/login"

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("username"); v != "" {
			params["username"] = v
		}
		if v, _ := cmd.Flags().GetString("password"); v != "" {
			params["password"] = v
		}
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	userLoginCmd.Flags().String("username", "", "The user name for login")
	userLoginCmd.Flags().String("password", "", "The password for login in clear text")
}

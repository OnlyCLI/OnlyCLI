// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/petstore/petstore-cli/runtime"
	"github.com/spf13/cobra"
)

var storeDeleteOrderCmd = &cobra.Command{
	Use:   "delete-order",
	Short: "Delete purchase order by identifier.",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/store/order/{orderId}"
		orderIdVal, _ := cmd.Flags().GetString("order-id")
		if orderIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "order-id")
		}
		path = strings.Replace(path, "{orderId}", orderIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("DELETE", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	storeDeleteOrderCmd.Flags().String("order-id", "", "ID of the order that needs to be deleted")
	_ = storeDeleteOrderCmd.MarkFlagRequired("order-id")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var checksListForSuiteCmd = &cobra.Command{
	Use:   "list-for-suite",
	Short: "List check runs in a check suite",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		checkSuiteIdVal, _ := cmd.Flags().GetString("check-suite-id")
		if checkSuiteIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "check-suite-id")
		}
		path = strings.Replace(path, "{check_suite_id}", checkSuiteIdVal, 1)

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("check-name"); v != "" {
			params["check_name"] = v
		}
		if v, _ := cmd.Flags().GetString("status"); v != "" {
			params["status"] = v
		}
		if v, _ := cmd.Flags().GetString("filter"); v != "" {
			params["filter"] = v
		}
		if v, _ := cmd.Flags().GetString("per-page"); v != "" {
			params["per_page"] = v
		}
		if v, _ := cmd.Flags().GetString("page"); v != "" {
			params["page"] = v
		}
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	checksListForSuiteCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = checksListForSuiteCmd.MarkFlagRequired("owner")
	checksListForSuiteCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = checksListForSuiteCmd.MarkFlagRequired("repo")
	checksListForSuiteCmd.Flags().String("check-suite-id", "", "The unique identifier of the check suite.")
	_ = checksListForSuiteCmd.MarkFlagRequired("check-suite-id")
	checksListForSuiteCmd.Flags().String("check-name", "", "Returns check runs with the specified `name`.")
	checksListForSuiteCmd.Flags().String("status", "", "Returns check runs with the specified `status`.")
	_ = checksListForSuiteCmd.RegisterFlagCompletionFunc("status", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"queued", "in_progress", "completed"}, cobra.ShellCompDirectiveNoFileComp
	})
	checksListForSuiteCmd.Flags().String("filter", "latest", "Filters check runs by their `completed_at` timestamp. `latest` returns the most recent check runs.")
	_ = checksListForSuiteCmd.RegisterFlagCompletionFunc("filter", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"latest", "all"}, cobra.ShellCompDirectiveNoFileComp
	})
	checksListForSuiteCmd.Flags().String("per-page", "30", "The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
	checksListForSuiteCmd.Flags().String("page", "1", "The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
}

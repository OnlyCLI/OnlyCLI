// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateUsingTemplateCmd = &cobra.Command{
	Use:   "create-using-template",
	Short: "Create a repository using a template",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{template_owner}/{template_repo}/generate"
		templateOwnerVal, _ := cmd.Flags().GetString("template-owner")
		if templateOwnerVal == "" {
			return fmt.Errorf("required flag --%s not set", "template-owner")
		}
		path = strings.Replace(path, "{template_owner}", templateOwnerVal, 1)
		templateRepoVal, _ := cmd.Flags().GetString("template-repo")
		if templateRepoVal == "" {
			return fmt.Errorf("required flag --%s not set", "template-repo")
		}
		path = strings.Replace(path, "{template_repo}", templateRepoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "owner", JSONPath: "owner", Type: "string"},
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "description", JSONPath: "description", Type: "string"},
			{FlagName: "include-all-branches", JSONPath: "include_all_branches", Type: "boolean"},
			{FlagName: "private", JSONPath: "private", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateUsingTemplateCmd.Flags().String("template-owner", "", "The account owner of the template repository. The name is not case sensitive.")
	_ = reposCreateUsingTemplateCmd.MarkFlagRequired("template-owner")
	reposCreateUsingTemplateCmd.Flags().String("template-repo", "", "The name of the template repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateUsingTemplateCmd.MarkFlagRequired("template-repo")
	reposCreateUsingTemplateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateUsingTemplateCmd.Flags().String("owner", "", "The organization or person who will own the new repository. To create a new repository in an organization, the authenticated user must be a member of the specified organization.")
	reposCreateUsingTemplateCmd.Flags().String("name", "", "The name of the new repository.")
	_ = reposCreateUsingTemplateCmd.MarkFlagRequired("name")
	reposCreateUsingTemplateCmd.Flags().String("description", "", "A short description of the new repository.")
	reposCreateUsingTemplateCmd.Flags().String("include-all-branches", "false", "Set to `true` to include the directory structure and files from all branches in the template repository, and not just the default branch. Default: `false`.")
	reposCreateUsingTemplateCmd.Flags().String("private", "false", "Either `true` to create a new private repository or `false` to create a new public one.")
}

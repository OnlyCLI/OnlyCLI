// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateForkCmd = &cobra.Command{
	Use:   "create-fork",
	Short: "Create a fork",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/forks"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "organization", JSONPath: "organization", Type: "string"},
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "default-branch-only", JSONPath: "default_branch_only", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateForkCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreateForkCmd.MarkFlagRequired("owner")
	reposCreateForkCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateForkCmd.MarkFlagRequired("repo")
	reposCreateForkCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateForkCmd.Flags().String("organization", "", "Optional parameter to specify the organization name if forking into an organization.")
	reposCreateForkCmd.Flags().String("name", "", "When forking from an existing repository, a new name for the fork.")
	reposCreateForkCmd.Flags().String("default-branch-only", "", "When forking from an existing repository, fork with only the default branch.")
}

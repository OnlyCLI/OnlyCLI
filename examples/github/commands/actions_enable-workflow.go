// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var actionsEnableWorkflowCmd = &cobra.Command{
	Use:   "enable-workflow",
	Short: "Enable a workflow",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		workflowIdVal, _ := cmd.Flags().GetString("workflow-id")
		if workflowIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "workflow-id")
		}
		path = strings.Replace(path, "{workflow_id}", workflowIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("PUT", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	actionsEnableWorkflowCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = actionsEnableWorkflowCmd.MarkFlagRequired("owner")
	actionsEnableWorkflowCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = actionsEnableWorkflowCmd.MarkFlagRequired("repo")
	actionsEnableWorkflowCmd.Flags().String("workflow-id", "", "The ID of the workflow. You can also pass the workflow file name as a string.")
	_ = actionsEnableWorkflowCmd.MarkFlagRequired("workflow-id")
}

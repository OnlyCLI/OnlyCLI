// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var copilotAddCopilotSeatsForTeamsCmd = &cobra.Command{
	Use:   "add-copilot-seats-for-teams",
	Short: "Add teams to the Copilot subscription for an organization",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/orgs/{org}/copilot/billing/selected_teams"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "selected-teams", JSONPath: "selected_teams", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	copilotAddCopilotSeatsForTeamsCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = copilotAddCopilotSeatsForTeamsCmd.MarkFlagRequired("org")
	copilotAddCopilotSeatsForTeamsCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	copilotAddCopilotSeatsForTeamsCmd.Flags().String("selected-teams", "", "List of team names within the organization to which to grant access to GitHub Copilot.")
	_ = copilotAddCopilotSeatsForTeamsCmd.MarkFlagRequired("selected-teams")
}

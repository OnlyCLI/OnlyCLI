// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var teamsCheckPermissionsForRepoLegacyCmd = &cobra.Command{
	Use:   "check-permissions-for-repo-legacy",
	Short: "Check team permissions for a repository (Legacy)",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/teams/{team_id}/repos/{owner}/{repo}"
		teamIdVal, _ := cmd.Flags().GetString("team-id")
		if teamIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "team-id")
		}
		path = strings.Replace(path, "{team_id}", teamIdVal, 1)
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	teamsCheckPermissionsForRepoLegacyCmd.Flags().String("team-id", "", "The unique identifier of the team.")
	_ = teamsCheckPermissionsForRepoLegacyCmd.MarkFlagRequired("team-id")
	teamsCheckPermissionsForRepoLegacyCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = teamsCheckPermissionsForRepoLegacyCmd.MarkFlagRequired("owner")
	teamsCheckPermissionsForRepoLegacyCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = teamsCheckPermissionsForRepoLegacyCmd.MarkFlagRequired("repo")
}

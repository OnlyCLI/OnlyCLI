// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var issuesUpdateCmd = &cobra.Command{
	Use:   "update",
	Short: "Update an issue",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/issues/{issue_number}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		issueNumberVal, _ := cmd.Flags().GetString("issue-number")
		if issueNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "issue-number")
		}
		path = strings.Replace(path, "{issue_number}", issueNumberVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "title", JSONPath: "title", Type: "string"},
			{FlagName: "body", JSONPath: "body", Type: "string"},
			{FlagName: "assignee", JSONPath: "assignee", Type: "string"},
			{FlagName: "state", JSONPath: "state", Type: "string"},
			{FlagName: "state-reason", JSONPath: "state_reason", Type: "string"},
			{FlagName: "milestone", JSONPath: "milestone", Type: "string"},
			{FlagName: "labels", JSONPath: "labels", Type: "array"},
			{FlagName: "assignees", JSONPath: "assignees", Type: "array"},
			{FlagName: "issue-field-values", JSONPath: "issue_field_values", Type: "array"},
			{FlagName: "type", JSONPath: "type", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	issuesUpdateCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = issuesUpdateCmd.MarkFlagRequired("owner")
	issuesUpdateCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = issuesUpdateCmd.MarkFlagRequired("repo")
	issuesUpdateCmd.Flags().String("issue-number", "", "The number that identifies the issue.")
	_ = issuesUpdateCmd.MarkFlagRequired("issue-number")
	issuesUpdateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	issuesUpdateCmd.Flags().String("title", "", "The title of the issue.")
	issuesUpdateCmd.Flags().String("body", "", "The contents of the issue.")
	issuesUpdateCmd.Flags().String("assignee", "", "Username to assign to this issue. **This field is closing down.**")
	issuesUpdateCmd.Flags().String("state", "", "The open or closed state of the issue.")
	_ = issuesUpdateCmd.RegisterFlagCompletionFunc("state", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"open", "closed"}, cobra.ShellCompDirectiveNoFileComp
	})
	issuesUpdateCmd.Flags().String("state-reason", "", "The reason for the state change. Ignored unless `state` is changed.")
	_ = issuesUpdateCmd.RegisterFlagCompletionFunc("state-reason", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"completed", "not_planned", "duplicate", "reopened"}, cobra.ShellCompDirectiveNoFileComp
	})
	issuesUpdateCmd.Flags().String("milestone", "", "")
	issuesUpdateCmd.Flags().String("labels", "", "Labels to associate with this issue. Pass one or more labels to _replace_ the set of labels on this issue. Send an empty array (`[]`) to clear all labels from the issue. Only users with push access can set labels for issues. Without push access to the repository, label changes are silently dropped.")
	issuesUpdateCmd.Flags().String("assignees", "", "Usernames to assign to this issue. Pass one or more user logins to _replace_ the set of assignees on this issue. Send an empty array (`[]`) to clear all assignees from the issue. Only users with push access can set assignees for new issues. Without push access to the repository, assignee changes are silently dropped.")
	issuesUpdateCmd.Flags().String("issue-field-values", "", "An array of issue field values to set on this issue. Each field value must include the field ID and the value to set. Only users with push access can set field values for issues")
	issuesUpdateCmd.Flags().String("type", "", "The name of the issue type to associate with this issue or use `null` to remove the current issue type. Only users with push access can set the type for issues. Without push access to the repository, type changes are silently dropped.")
}

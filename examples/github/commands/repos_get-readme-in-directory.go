// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposGetReadmeInDirectoryCmd = &cobra.Command{
	Use:   "get-readme-in-directory",
	Short: "Get a repository README for a directory",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/readme/{dir}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		dirVal, _ := cmd.Flags().GetString("dir")
		if dirVal == "" {
			return fmt.Errorf("required flag --%s not set", "dir")
		}
		path = strings.Replace(path, "{dir}", dirVal, 1)

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("ref"); v != "" {
			params["ref"] = v
		}
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposGetReadmeInDirectoryCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposGetReadmeInDirectoryCmd.MarkFlagRequired("owner")
	reposGetReadmeInDirectoryCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposGetReadmeInDirectoryCmd.MarkFlagRequired("repo")
	reposGetReadmeInDirectoryCmd.Flags().String("dir", "", "The alternate path to look for a README file")
	_ = reposGetReadmeInDirectoryCmd.MarkFlagRequired("dir")
	reposGetReadmeInDirectoryCmd.Flags().String("ref", "", "The name of the commit/branch/tag. Default: the repository’s default branch.")
}

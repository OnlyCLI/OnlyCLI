// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var usersListAttestationsBulkCmd = &cobra.Command{
	Use:   "list-attestations-bulk",
	Short: "List attestations by bulk subject digests",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/users/{username}/attestations/bulk-list"
		usernameVal, _ := cmd.Flags().GetString("username")
		if usernameVal == "" {
			return fmt.Errorf("required flag --%s not set", "username")
		}
		path = strings.Replace(path, "{username}", usernameVal, 1)

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("per-page"); v != "" {
			params["per_page"] = v
		}
		if v, _ := cmd.Flags().GetString("before"); v != "" {
			params["before"] = v
		}
		if v, _ := cmd.Flags().GetString("after"); v != "" {
			params["after"] = v
		}
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "subject-digests", JSONPath: "subject_digests", Type: "array"},
			{FlagName: "predicate-type", JSONPath: "predicate_type", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	usersListAttestationsBulkCmd.Flags().String("per-page", "30", "The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
	usersListAttestationsBulkCmd.Flags().String("before", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
	usersListAttestationsBulkCmd.Flags().String("after", "", "A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
	usersListAttestationsBulkCmd.Flags().String("username", "", "The handle for the GitHub user account.")
	_ = usersListAttestationsBulkCmd.MarkFlagRequired("username")
	usersListAttestationsBulkCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	usersListAttestationsBulkCmd.Flags().String("subject-digests", "", "List of subject digests to fetch attestations for.")
	_ = usersListAttestationsBulkCmd.MarkFlagRequired("subject-digests")
	usersListAttestationsBulkCmd.Flags().String("predicate-type", "", "Optional filter for fetching attestations with a given predicate type. This option accepts `provenance`, `sbom`, `release`, or freeform text for custom predicate types.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var billingUpdateBudgetOrgCmd = &cobra.Command{
	Use:   "update-budget-org",
	Short: "Update a budget for an organization",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/organizations/{org}/settings/billing/budgets/{budget_id}"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)
		budgetIdVal, _ := cmd.Flags().GetString("budget-id")
		if budgetIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "budget-id")
		}
		path = strings.Replace(path, "{budget_id}", budgetIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "budget-amount", JSONPath: "budget_amount", Type: "integer"},
			{FlagName: "prevent-further-usage", JSONPath: "prevent_further_usage", Type: "boolean"},
			{FlagName: "budget-alerting.will-alert", JSONPath: "budget_alerting.will_alert", Type: "boolean"},
			{FlagName: "budget-alerting.alert-recipients", JSONPath: "budget_alerting.alert_recipients", Type: "array"},
			{FlagName: "budget-scope", JSONPath: "budget_scope", Type: "string"},
			{FlagName: "budget-entity-name", JSONPath: "budget_entity_name", Type: "string"},
			{FlagName: "budget-type", JSONPath: "budget_type", Type: "string"},
			{FlagName: "budget-product-sku", JSONPath: "budget_product_sku", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	billingUpdateBudgetOrgCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = billingUpdateBudgetOrgCmd.MarkFlagRequired("org")
	billingUpdateBudgetOrgCmd.Flags().String("budget-id", "", "The ID corresponding to the budget.")
	_ = billingUpdateBudgetOrgCmd.MarkFlagRequired("budget-id")
	billingUpdateBudgetOrgCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	billingUpdateBudgetOrgCmd.Flags().String("budget-amount", "", "The budget amount in whole dollars. For license-based products, this represents the number of licenses.")
	billingUpdateBudgetOrgCmd.Flags().String("prevent-further-usage", "", "Whether to prevent additional spending once the budget is exceeded")
	billingUpdateBudgetOrgCmd.Flags().String("budget-alerting.will-alert", "", "Whether alerts are enabled for this budget")
	billingUpdateBudgetOrgCmd.Flags().String("budget-alerting.alert-recipients", "", "Array of user login names who will receive alerts")
	billingUpdateBudgetOrgCmd.Flags().String("budget-scope", "", "The scope of the budget")
	_ = billingUpdateBudgetOrgCmd.RegisterFlagCompletionFunc("budget-scope", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"enterprise", "organization", "repository", "cost_center"}, cobra.ShellCompDirectiveNoFileComp
	})
	billingUpdateBudgetOrgCmd.Flags().String("budget-entity-name", "", "The name of the entity to apply the budget to")
	billingUpdateBudgetOrgCmd.Flags().String("budget-type", "", "The type of pricing for the budget")
	billingUpdateBudgetOrgCmd.Flags().String("budget-product-sku", "", "A single product or SKU that will be covered in the budget")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reactionsCreateForReleaseCmd = &cobra.Command{
	Use:   "create-for-release",
	Short: "Create reaction for a release",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/releases/{release_id}/reactions"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		releaseIdVal, _ := cmd.Flags().GetString("release-id")
		if releaseIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "release-id")
		}
		path = strings.Replace(path, "{release_id}", releaseIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "content", JSONPath: "content", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reactionsCreateForReleaseCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reactionsCreateForReleaseCmd.MarkFlagRequired("owner")
	reactionsCreateForReleaseCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reactionsCreateForReleaseCmd.MarkFlagRequired("repo")
	reactionsCreateForReleaseCmd.Flags().String("release-id", "", "The unique identifier of the release.")
	_ = reactionsCreateForReleaseCmd.MarkFlagRequired("release-id")
	reactionsCreateForReleaseCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reactionsCreateForReleaseCmd.Flags().String("content", "", "The [reaction type](https://docs.github.com/rest/reactions/reactions#about-reactions) to add to the release.")
	_ = reactionsCreateForReleaseCmd.RegisterFlagCompletionFunc("content", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"+1", "laugh", "heart", "hooray", "rocket", "eyes"}, cobra.ShellCompDirectiveNoFileComp
	})
	_ = reactionsCreateForReleaseCmd.MarkFlagRequired("content")
}

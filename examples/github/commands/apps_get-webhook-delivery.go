// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var appsGetWebhookDeliveryCmd = &cobra.Command{
	Use:   "get-webhook-delivery",
	Short: "Get a delivery for an app webhook",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/app/hook/deliveries/{delivery_id}"
		deliveryIdVal, _ := cmd.Flags().GetString("delivery-id")
		if deliveryIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "delivery-id")
		}
		path = strings.Replace(path, "{delivery_id}", deliveryIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	appsGetWebhookDeliveryCmd.Flags().String("delivery-id", "", "")
	_ = appsGetWebhookDeliveryCmd.MarkFlagRequired("delivery-id")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var issuesDeleteCommentCmd = &cobra.Command{
	Use:   "delete-comment",
	Short: "Delete an issue comment",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/issues/comments/{comment_id}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		commentIdVal, _ := cmd.Flags().GetString("comment-id")
		if commentIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "comment-id")
		}
		path = strings.Replace(path, "{comment_id}", commentIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("DELETE", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	issuesDeleteCommentCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = issuesDeleteCommentCmd.MarkFlagRequired("owner")
	issuesDeleteCommentCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = issuesDeleteCommentCmd.MarkFlagRequired("repo")
	issuesDeleteCommentCmd.Flags().String("comment-id", "", "The unique identifier of the comment.")
	_ = issuesDeleteCommentCmd.MarkFlagRequired("comment-id")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var projectsAddFieldForUserCmd = &cobra.Command{
	Use:   "add-field-for-user",
	Short: "Add field to user owned project",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/users/{username}/projectsV2/{project_number}/fields"
		usernameVal, _ := cmd.Flags().GetString("username")
		if usernameVal == "" {
			return fmt.Errorf("required flag --%s not set", "username")
		}
		path = strings.Replace(path, "{username}", usernameVal, 1)
		projectNumberVal, _ := cmd.Flags().GetString("project-number")
		if projectNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "project-number")
		}
		path = strings.Replace(path, "{project_number}", projectNumberVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	projectsAddFieldForUserCmd.Flags().String("username", "", "The handle for the GitHub user account.")
	_ = projectsAddFieldForUserCmd.MarkFlagRequired("username")
	projectsAddFieldForUserCmd.Flags().String("project-number", "", "The project's number.")
	_ = projectsAddFieldForUserCmd.MarkFlagRequired("project-number")
	projectsAddFieldForUserCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var billingGetBudgetOrgCmd = &cobra.Command{
	Use:   "get-budget-org",
	Short: "Get a budget by ID for an organization",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/organizations/{org}/settings/billing/budgets/{budget_id}"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)
		budgetIdVal, _ := cmd.Flags().GetString("budget-id")
		if budgetIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "budget-id")
		}
		path = strings.Replace(path, "{budget_id}", budgetIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	billingGetBudgetOrgCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = billingGetBudgetOrgCmd.MarkFlagRequired("org")
	billingGetBudgetOrgCmd.Flags().String("budget-id", "", "The ID corresponding to the budget.")
	_ = billingGetBudgetOrgCmd.MarkFlagRequired("budget-id")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var pullsSubmitReviewCmd = &cobra.Command{
	Use:   "submit-review",
	Short: "Submit a review for a pull request",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		pullNumberVal, _ := cmd.Flags().GetString("pull-number")
		if pullNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "pull-number")
		}
		path = strings.Replace(path, "{pull_number}", pullNumberVal, 1)
		reviewIdVal, _ := cmd.Flags().GetString("review-id")
		if reviewIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "review-id")
		}
		path = strings.Replace(path, "{review_id}", reviewIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "body", JSONPath: "body", Type: "string"},
			{FlagName: "event", JSONPath: "event", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	pullsSubmitReviewCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = pullsSubmitReviewCmd.MarkFlagRequired("owner")
	pullsSubmitReviewCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = pullsSubmitReviewCmd.MarkFlagRequired("repo")
	pullsSubmitReviewCmd.Flags().String("pull-number", "", "The number that identifies the pull request.")
	_ = pullsSubmitReviewCmd.MarkFlagRequired("pull-number")
	pullsSubmitReviewCmd.Flags().String("review-id", "", "The unique identifier of the review.")
	_ = pullsSubmitReviewCmd.MarkFlagRequired("review-id")
	pullsSubmitReviewCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	pullsSubmitReviewCmd.Flags().String("body", "", "The body text of the pull request review")
	pullsSubmitReviewCmd.Flags().String("event", "", "The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. When you leave this blank, the API returns _HTTP 422 (Unrecognizable entity)_ and sets the review action state to `PENDING`, which means you will need to re-submit the pull request review using a review action.")
	_ = pullsSubmitReviewCmd.RegisterFlagCompletionFunc("event", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"APPROVE", "REQUEST_CHANGES", "COMMENT"}, cobra.ShellCompDirectiveNoFileComp
	})
	_ = pullsSubmitReviewCmd.MarkFlagRequired("event")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var activityMarkNotificationsAsReadCmd = &cobra.Command{
	Use:   "mark-notifications-as-read",
	Short: "Mark notifications as read",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/notifications"

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "last-read-at", JSONPath: "last_read_at", Type: "string"},
			{FlagName: "read", JSONPath: "read", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PUT", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	activityMarkNotificationsAsReadCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	activityMarkNotificationsAsReadCmd.Flags().String("last-read-at", "", "Describes the last point that notifications were checked. Anything updated since this time will not be marked as read. If you omit this parameter, all notifications are marked as read. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Default: The current timestamp.")
	activityMarkNotificationsAsReadCmd.Flags().String("read", "", "Whether the notification has been read.")
}

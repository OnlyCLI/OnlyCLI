// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var checksUpdateCmd = &cobra.Command{
	Use:   "update",
	Short: "Update a check run",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/check-runs/{check_run_id}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		checkRunIdVal, _ := cmd.Flags().GetString("check-run-id")
		if checkRunIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "check-run-id")
		}
		path = strings.Replace(path, "{check_run_id}", checkRunIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "details-url", JSONPath: "details_url", Type: "string"},
			{FlagName: "external-id", JSONPath: "external_id", Type: "string"},
			{FlagName: "started-at", JSONPath: "started_at", Type: "string"},
			{FlagName: "status", JSONPath: "status", Type: "string"},
			{FlagName: "conclusion", JSONPath: "conclusion", Type: "string"},
			{FlagName: "completed-at", JSONPath: "completed_at", Type: "string"},
			{FlagName: "output.title", JSONPath: "output.title", Type: "string"},
			{FlagName: "output.summary", JSONPath: "output.summary", Type: "string"},
			{FlagName: "output.text", JSONPath: "output.text", Type: "string"},
			{FlagName: "output.annotations", JSONPath: "output.annotations", Type: "array"},
			{FlagName: "output.images", JSONPath: "output.images", Type: "array"},
			{FlagName: "actions", JSONPath: "actions", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	checksUpdateCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = checksUpdateCmd.MarkFlagRequired("owner")
	checksUpdateCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = checksUpdateCmd.MarkFlagRequired("repo")
	checksUpdateCmd.Flags().String("check-run-id", "", "The unique identifier of the check run.")
	_ = checksUpdateCmd.MarkFlagRequired("check-run-id")
	checksUpdateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	checksUpdateCmd.Flags().String("name", "", "The name of the check. For example, \"code-coverage\".")
	checksUpdateCmd.Flags().String("details-url", "", "The URL of the integrator's site that has the full details of the check.")
	checksUpdateCmd.Flags().String("external-id", "", "A reference for the run on the integrator's system.")
	checksUpdateCmd.Flags().String("started-at", "", "This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
	checksUpdateCmd.Flags().String("status", "", "The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`.")
	_ = checksUpdateCmd.RegisterFlagCompletionFunc("status", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"queued", "in_progress", "completed", "waiting", "requested", "pending"}, cobra.ShellCompDirectiveNoFileComp
	})
	checksUpdateCmd.Flags().String("conclusion", "", "**Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this.")
	_ = checksUpdateCmd.RegisterFlagCompletionFunc("conclusion", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"action_required", "cancelled", "failure", "neutral", "success", "skipped", "stale", "timed_out"}, cobra.ShellCompDirectiveNoFileComp
	})
	checksUpdateCmd.Flags().String("completed-at", "", "The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
	checksUpdateCmd.Flags().String("output.title", "", "**Required**.")
	checksUpdateCmd.Flags().String("output.summary", "", "Can contain Markdown.")
	_ = checksUpdateCmd.MarkFlagRequired("output.summary")
	checksUpdateCmd.Flags().String("output.text", "", "Can contain Markdown.")
	checksUpdateCmd.Flags().String("output.annotations", "", "Adds information from your analysis to specific lines of code. Annotations are visible in GitHub's pull request UI. Annotations are visible in GitHub's pull request UI. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about annotations in the UI, see \"[About status checks](https://docs.github.com/articles/about-status-checks#checks)\".")
	checksUpdateCmd.Flags().String("output.images", "", "Adds images to the output displayed in the GitHub pull request UI.")
	checksUpdateCmd.Flags().String("actions", "", "Possible further actions the integrator can perform, which a user may trigger. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see \"[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions).\"")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var teamsRemoveMemberLegacyCmd = &cobra.Command{
	Use:   "remove-member-legacy",
	Short: "Remove team member (Legacy)",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/teams/{team_id}/members/{username}"
		teamIdVal, _ := cmd.Flags().GetString("team-id")
		if teamIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "team-id")
		}
		path = strings.Replace(path, "{team_id}", teamIdVal, 1)
		usernameVal, _ := cmd.Flags().GetString("username")
		if usernameVal == "" {
			return fmt.Errorf("required flag --%s not set", "username")
		}
		path = strings.Replace(path, "{username}", usernameVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("DELETE", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	teamsRemoveMemberLegacyCmd.Flags().String("team-id", "", "The unique identifier of the team.")
	_ = teamsRemoveMemberLegacyCmd.MarkFlagRequired("team-id")
	teamsRemoveMemberLegacyCmd.Flags().String("username", "", "The handle for the GitHub user account.")
	_ = teamsRemoveMemberLegacyCmd.MarkFlagRequired("username")
}

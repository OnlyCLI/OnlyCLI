// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposGetDeployKeyCmd = &cobra.Command{
	Use:   "get-deploy-key",
	Short: "Get a deploy key",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/keys/{key_id}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		keyIdVal, _ := cmd.Flags().GetString("key-id")
		if keyIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "key-id")
		}
		path = strings.Replace(path, "{key_id}", keyIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposGetDeployKeyCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposGetDeployKeyCmd.MarkFlagRequired("owner")
	reposGetDeployKeyCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposGetDeployKeyCmd.MarkFlagRequired("repo")
	reposGetDeployKeyCmd.Flags().String("key-id", "", "The unique identifier of the key.")
	_ = reposGetDeployKeyCmd.MarkFlagRequired("key-id")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var gitCreateRefCmd = &cobra.Command{
	Use:   "create-ref",
	Short: "Create a reference",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/git/refs"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "ref", JSONPath: "ref", Type: "string"},
			{FlagName: "sha", JSONPath: "sha", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	gitCreateRefCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = gitCreateRefCmd.MarkFlagRequired("owner")
	gitCreateRefCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = gitCreateRefCmd.MarkFlagRequired("repo")
	gitCreateRefCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	gitCreateRefCmd.Flags().String("ref", "", "The name of the fully qualified reference (ie: `refs/heads/master`). If it doesn't start with 'refs' and have at least two slashes, it will be rejected.")
	_ = gitCreateRefCmd.MarkFlagRequired("ref")
	gitCreateRefCmd.Flags().String("sha", "", "The SHA1 value for this reference.")
	_ = gitCreateRefCmd.MarkFlagRequired("sha")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var orgsCreateWebhookCmd = &cobra.Command{
	Use:   "create-webhook",
	Short: "Create an organization webhook",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/orgs/{org}/hooks"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "config.url", JSONPath: "config.url", Type: "string"},
			{FlagName: "config.content-type", JSONPath: "config.content_type", Type: "string"},
			{FlagName: "config.secret", JSONPath: "config.secret", Type: "string"},
			{FlagName: "config.insecure-ssl", JSONPath: "config.insecure_ssl", Type: "string"},
			{FlagName: "config.username", JSONPath: "config.username", Type: "string"},
			{FlagName: "config.password", JSONPath: "config.password", Type: "string"},
			{FlagName: "events", JSONPath: "events", Type: "array"},
			{FlagName: "active", JSONPath: "active", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	orgsCreateWebhookCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = orgsCreateWebhookCmd.MarkFlagRequired("org")
	orgsCreateWebhookCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	orgsCreateWebhookCmd.Flags().String("name", "", "Must be passed as \"web\".")
	_ = orgsCreateWebhookCmd.MarkFlagRequired("name")
	orgsCreateWebhookCmd.Flags().String("config.url", "", "The URL to which the payloads will be delivered.")
	_ = orgsCreateWebhookCmd.MarkFlagRequired("config.url")
	orgsCreateWebhookCmd.Flags().String("config.content-type", "", "The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.")
	orgsCreateWebhookCmd.Flags().String("config.secret", "", "If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).")
	orgsCreateWebhookCmd.Flags().String("config.insecure-ssl", "", "")
	orgsCreateWebhookCmd.Flags().String("config.username", "", "")
	orgsCreateWebhookCmd.Flags().String("config.password", "", "")
	orgsCreateWebhookCmd.Flags().String("events", "", "Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for. Set to `[\"*\"]` to receive all possible events.")
	orgsCreateWebhookCmd.Flags().String("active", "true", "Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreatePagesDeploymentCmd = &cobra.Command{
	Use:   "create-pages-deployment",
	Short: "Create a GitHub Pages deployment",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/pages/deployments"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "artifact-id", JSONPath: "artifact_id", Type: "number"},
			{FlagName: "artifact-url", JSONPath: "artifact_url", Type: "string"},
			{FlagName: "environment", JSONPath: "environment", Type: "string"},
			{FlagName: "pages-build-version", JSONPath: "pages_build_version", Type: "string"},
			{FlagName: "oidc-token", JSONPath: "oidc_token", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreatePagesDeploymentCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreatePagesDeploymentCmd.MarkFlagRequired("owner")
	reposCreatePagesDeploymentCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreatePagesDeploymentCmd.MarkFlagRequired("repo")
	reposCreatePagesDeploymentCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreatePagesDeploymentCmd.Flags().String("artifact-id", "", "The ID of an artifact that contains the .zip or .tar of static assets to deploy. The artifact belongs to the repository. Either `artifact_id` or `artifact_url` are required.")
	reposCreatePagesDeploymentCmd.Flags().String("artifact-url", "", "The URL of an artifact that contains the .zip or .tar of static assets to deploy. The artifact belongs to the repository. Either `artifact_id` or `artifact_url` are required.")
	reposCreatePagesDeploymentCmd.Flags().String("environment", "github-pages", "The target environment for this GitHub Pages deployment.")
	reposCreatePagesDeploymentCmd.Flags().String("pages-build-version", "GITHUB_SHA", "A unique string that represents the version of the build for this deployment.")
	_ = reposCreatePagesDeploymentCmd.MarkFlagRequired("pages-build-version")
	reposCreatePagesDeploymentCmd.Flags().String("oidc-token", "", "The OIDC token issued by GitHub Actions certifying the origin of the deployment.")
	_ = reposCreatePagesDeploymentCmd.MarkFlagRequired("oidc-token")
}

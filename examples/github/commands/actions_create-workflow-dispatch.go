// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var actionsCreateWorkflowDispatchCmd = &cobra.Command{
	Use:   "create-workflow-dispatch",
	Short: "Create a workflow dispatch event",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		workflowIdVal, _ := cmd.Flags().GetString("workflow-id")
		if workflowIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "workflow-id")
		}
		path = strings.Replace(path, "{workflow_id}", workflowIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "ref", JSONPath: "ref", Type: "string"},
			{FlagName: "inputs", JSONPath: "inputs", Type: "object"},
			{FlagName: "return-run-details", JSONPath: "return_run_details", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	actionsCreateWorkflowDispatchCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = actionsCreateWorkflowDispatchCmd.MarkFlagRequired("owner")
	actionsCreateWorkflowDispatchCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = actionsCreateWorkflowDispatchCmd.MarkFlagRequired("repo")
	actionsCreateWorkflowDispatchCmd.Flags().String("workflow-id", "", "The ID of the workflow. You can also pass the workflow file name as a string.")
	_ = actionsCreateWorkflowDispatchCmd.MarkFlagRequired("workflow-id")
	actionsCreateWorkflowDispatchCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	actionsCreateWorkflowDispatchCmd.Flags().String("ref", "", "The git reference for the workflow. The reference can be a branch or tag name.")
	_ = actionsCreateWorkflowDispatchCmd.MarkFlagRequired("ref")
	actionsCreateWorkflowDispatchCmd.Flags().String("inputs", "", "Input keys and values configured in the workflow file. The maximum number of properties is 25. Any default properties configured in the workflow file will be used when `inputs` are omitted.")
	actionsCreateWorkflowDispatchCmd.Flags().String("return-run-details", "", "Whether the response should include the workflow run ID and URLs.")
}

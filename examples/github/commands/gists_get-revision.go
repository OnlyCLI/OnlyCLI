// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var gistsGetRevisionCmd = &cobra.Command{
	Use:   "get-revision",
	Short: "Get a gist revision",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/gists/{gist_id}/{sha}"
		gistIdVal, _ := cmd.Flags().GetString("gist-id")
		if gistIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "gist-id")
		}
		path = strings.Replace(path, "{gist_id}", gistIdVal, 1)
		shaVal, _ := cmd.Flags().GetString("sha")
		if shaVal == "" {
			return fmt.Errorf("required flag --%s not set", "sha")
		}
		path = strings.Replace(path, "{sha}", shaVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	gistsGetRevisionCmd.Flags().String("gist-id", "", "The unique identifier of the gist.")
	_ = gistsGetRevisionCmd.MarkFlagRequired("gist-id")
	gistsGetRevisionCmd.Flags().String("sha", "", "")
	_ = gistsGetRevisionCmd.MarkFlagRequired("sha")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var orgsListInvitationTeamsCmd = &cobra.Command{
	Use:   "list-invitation-teams",
	Short: "List organization invitation teams",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/orgs/{org}/invitations/{invitation_id}/teams"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)
		invitationIdVal, _ := cmd.Flags().GetString("invitation-id")
		if invitationIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "invitation-id")
		}
		path = strings.Replace(path, "{invitation_id}", invitationIdVal, 1)

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("per-page"); v != "" {
			params["per_page"] = v
		}
		if v, _ := cmd.Flags().GetString("page"); v != "" {
			params["page"] = v
		}
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	orgsListInvitationTeamsCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = orgsListInvitationTeamsCmd.MarkFlagRequired("org")
	orgsListInvitationTeamsCmd.Flags().String("invitation-id", "", "The unique identifier of the invitation.")
	_ = orgsListInvitationTeamsCmd.MarkFlagRequired("invitation-id")
	orgsListInvitationTeamsCmd.Flags().String("per-page", "30", "The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
	orgsListInvitationTeamsCmd.Flags().String("page", "1", "The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
}

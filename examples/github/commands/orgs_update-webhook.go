// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var orgsUpdateWebhookCmd = &cobra.Command{
	Use:   "update-webhook",
	Short: "Update an organization webhook",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/orgs/{org}/hooks/{hook_id}"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)
		hookIdVal, _ := cmd.Flags().GetString("hook-id")
		if hookIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "hook-id")
		}
		path = strings.Replace(path, "{hook_id}", hookIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "config.url", JSONPath: "config.url", Type: "string"},
			{FlagName: "config.content-type", JSONPath: "config.content_type", Type: "string"},
			{FlagName: "config.secret", JSONPath: "config.secret", Type: "string"},
			{FlagName: "config.insecure-ssl", JSONPath: "config.insecure_ssl", Type: "string"},
			{FlagName: "events", JSONPath: "events", Type: "array"},
			{FlagName: "active", JSONPath: "active", Type: "boolean"},
			{FlagName: "name", JSONPath: "name", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	orgsUpdateWebhookCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = orgsUpdateWebhookCmd.MarkFlagRequired("org")
	orgsUpdateWebhookCmd.Flags().String("hook-id", "", "The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.")
	_ = orgsUpdateWebhookCmd.MarkFlagRequired("hook-id")
	orgsUpdateWebhookCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	orgsUpdateWebhookCmd.Flags().String("config.url", "", "The URL to which the payloads will be delivered.")
	_ = orgsUpdateWebhookCmd.MarkFlagRequired("config.url")
	orgsUpdateWebhookCmd.Flags().String("config.content-type", "", "The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.")
	orgsUpdateWebhookCmd.Flags().String("config.secret", "", "If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).")
	orgsUpdateWebhookCmd.Flags().String("config.insecure-ssl", "", "")
	orgsUpdateWebhookCmd.Flags().String("events", "", "Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for.")
	orgsUpdateWebhookCmd.Flags().String("active", "true", "Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.")
	orgsUpdateWebhookCmd.Flags().String("name", "", "")
}

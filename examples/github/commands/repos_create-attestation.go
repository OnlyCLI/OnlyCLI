// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateAttestationCmd = &cobra.Command{
	Use:   "create-attestation",
	Short: "Create an attestation",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/attestations"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "bundle.media-type", JSONPath: "bundle.mediaType", Type: "string"},
			{FlagName: "bundle.verification-material", JSONPath: "bundle.verificationMaterial", Type: "object"},
			{FlagName: "bundle.dsse-envelope", JSONPath: "bundle.dsseEnvelope", Type: "object"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateAttestationCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreateAttestationCmd.MarkFlagRequired("owner")
	reposCreateAttestationCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateAttestationCmd.MarkFlagRequired("repo")
	reposCreateAttestationCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateAttestationCmd.Flags().String("bundle.media-type", "", "")
	reposCreateAttestationCmd.Flags().String("bundle.verification-material", "", "")
	reposCreateAttestationCmd.Flags().String("bundle.dsse-envelope", "", "")
}

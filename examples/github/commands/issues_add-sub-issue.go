// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var issuesAddSubIssueCmd = &cobra.Command{
	Use:   "add-sub-issue",
	Short: "Add sub-issue",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/issues/{issue_number}/sub_issues"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		issueNumberVal, _ := cmd.Flags().GetString("issue-number")
		if issueNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "issue-number")
		}
		path = strings.Replace(path, "{issue_number}", issueNumberVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "sub-issue-id", JSONPath: "sub_issue_id", Type: "integer"},
			{FlagName: "replace-parent", JSONPath: "replace_parent", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	issuesAddSubIssueCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = issuesAddSubIssueCmd.MarkFlagRequired("owner")
	issuesAddSubIssueCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = issuesAddSubIssueCmd.MarkFlagRequired("repo")
	issuesAddSubIssueCmd.Flags().String("issue-number", "", "The number that identifies the issue.")
	_ = issuesAddSubIssueCmd.MarkFlagRequired("issue-number")
	issuesAddSubIssueCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	issuesAddSubIssueCmd.Flags().String("sub-issue-id", "", "The id of the sub-issue to add. The sub-issue must belong to the same repository owner as the parent issue")
	_ = issuesAddSubIssueCmd.MarkFlagRequired("sub-issue-id")
	issuesAddSubIssueCmd.Flags().String("replace-parent", "", "Option that, when true, instructs the operation to replace the sub-issues current parent issue")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateCommitStatusCmd = &cobra.Command{
	Use:   "create-commit-status",
	Short: "Create a commit status",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/statuses/{sha}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		shaVal, _ := cmd.Flags().GetString("sha")
		if shaVal == "" {
			return fmt.Errorf("required flag --%s not set", "sha")
		}
		path = strings.Replace(path, "{sha}", shaVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "state", JSONPath: "state", Type: "string"},
			{FlagName: "target-url", JSONPath: "target_url", Type: "string"},
			{FlagName: "description", JSONPath: "description", Type: "string"},
			{FlagName: "context", JSONPath: "context", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateCommitStatusCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreateCommitStatusCmd.MarkFlagRequired("owner")
	reposCreateCommitStatusCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateCommitStatusCmd.MarkFlagRequired("repo")
	reposCreateCommitStatusCmd.Flags().String("sha", "", "")
	_ = reposCreateCommitStatusCmd.MarkFlagRequired("sha")
	reposCreateCommitStatusCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateCommitStatusCmd.Flags().String("state", "", "The state of the status.")
	_ = reposCreateCommitStatusCmd.RegisterFlagCompletionFunc("state", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"error", "failure", "pending", "success"}, cobra.ShellCompDirectiveNoFileComp
	})
	_ = reposCreateCommitStatusCmd.MarkFlagRequired("state")
	reposCreateCommitStatusCmd.Flags().String("target-url", "", "The target URL to associate with this status. This URL will be linked from the GitHub UI to allow users to easily see the source of the status. For example, if your continuous integration system is posting build status, you would want to provide the deep link for the build output for this specific SHA: `http://ci.example.com/user/repo/build/sha`")
	reposCreateCommitStatusCmd.Flags().String("description", "", "A short description of the status.")
	reposCreateCommitStatusCmd.Flags().String("context", "default", "A string label to differentiate this status from the status of other systems. This field is case-insensitive.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var orgsCreateInvitationCmd = &cobra.Command{
	Use:   "create-invitation",
	Short: "Create an organization invitation",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/orgs/{org}/invitations"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "invitee-id", JSONPath: "invitee_id", Type: "integer"},
			{FlagName: "email", JSONPath: "email", Type: "string"},
			{FlagName: "role", JSONPath: "role", Type: "string"},
			{FlagName: "team-ids", JSONPath: "team_ids", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	orgsCreateInvitationCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = orgsCreateInvitationCmd.MarkFlagRequired("org")
	orgsCreateInvitationCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	orgsCreateInvitationCmd.Flags().String("invitee-id", "", "**Required unless you provide `email`**. GitHub user ID for the person you are inviting.")
	orgsCreateInvitationCmd.Flags().String("email", "", "**Required unless you provide `invitee_id`**. Email address of the person you are inviting, which can be an existing GitHub user.")
	orgsCreateInvitationCmd.Flags().String("role", "direct_member", "The role for the new member. * `admin` - Organization owners with full administrative rights to the organization and complete access to all repositories and teams. * `direct_member` - Non-owner organization members with ability to see other members and join teams by invitation. * `billing_manager` - Non-owner organization members with ability to manage the billing settings of your organization. * `reinstate` - The previous role assigned to the invitee before they were removed from your organization. Can be one of the roles listed above. Only works if the invitee was previously part of your organization.")
	_ = orgsCreateInvitationCmd.RegisterFlagCompletionFunc("role", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"admin", "direct_member", "billing_manager", "reinstate"}, cobra.ShellCompDirectiveNoFileComp
	})
	orgsCreateInvitationCmd.Flags().String("team-ids", "", "Specify IDs for the teams you want to invite new members to.")
}

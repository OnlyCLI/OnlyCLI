// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var pullsCreateCmd = &cobra.Command{
	Use:   "create",
	Short: "Create a pull request",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/pulls"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "title", JSONPath: "title", Type: "string"},
			{FlagName: "head", JSONPath: "head", Type: "string"},
			{FlagName: "head-repo", JSONPath: "head_repo", Type: "string"},
			{FlagName: "base", JSONPath: "base", Type: "string"},
			{FlagName: "body", JSONPath: "body", Type: "string"},
			{FlagName: "maintainer-can-modify", JSONPath: "maintainer_can_modify", Type: "boolean"},
			{FlagName: "draft", JSONPath: "draft", Type: "boolean"},
			{FlagName: "issue", JSONPath: "issue", Type: "integer"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	pullsCreateCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = pullsCreateCmd.MarkFlagRequired("owner")
	pullsCreateCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = pullsCreateCmd.MarkFlagRequired("repo")
	pullsCreateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	pullsCreateCmd.Flags().String("title", "", "The title of the new pull request. Required unless `issue` is specified.")
	pullsCreateCmd.Flags().String("head", "", "The name of the branch where your changes are implemented. For cross-repository pull requests in the same network, namespace `head` with a user like this: `username:branch`.")
	_ = pullsCreateCmd.MarkFlagRequired("head")
	pullsCreateCmd.Flags().String("head-repo", "", "The name of the repository where the changes in the pull request were made. This field is required for cross-repository pull requests if both repositories are owned by the same organization.")
	pullsCreateCmd.Flags().String("base", "", "The name of the branch you want the changes pulled into. This should be an existing branch on the current repository. You cannot submit a pull request to one repository that requests a merge to a base of another repository.")
	_ = pullsCreateCmd.MarkFlagRequired("base")
	pullsCreateCmd.Flags().String("body", "", "The contents of the pull request.")
	pullsCreateCmd.Flags().String("maintainer-can-modify", "", "Indicates whether [maintainers can modify](https://docs.github.com/articles/allowing-changes-to-a-pull-request-branch-created-from-a-fork/) the pull request.")
	pullsCreateCmd.Flags().String("draft", "", "Indicates whether the pull request is a draft. See \"[Draft Pull Requests](https://docs.github.com/articles/about-pull-requests#draft-pull-requests)\" in the GitHub Help documentation to learn more.")
	pullsCreateCmd.Flags().String("issue", "", "An issue in the repository to convert to a pull request. The issue title, body, and comments will become the title, body, and comments on the new pull request. Required unless `title` is specified.")
}

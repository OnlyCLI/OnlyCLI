// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposUpdateWebhookConfigForRepoCmd = &cobra.Command{
	Use:   "update-webhook-config-for-repo",
	Short: "Update a webhook configuration for a repository",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/hooks/{hook_id}/config"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		hookIdVal, _ := cmd.Flags().GetString("hook-id")
		if hookIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "hook-id")
		}
		path = strings.Replace(path, "{hook_id}", hookIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "url", JSONPath: "url", Type: "string"},
			{FlagName: "content-type", JSONPath: "content_type", Type: "string"},
			{FlagName: "secret", JSONPath: "secret", Type: "string"},
			{FlagName: "insecure-ssl", JSONPath: "insecure_ssl", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposUpdateWebhookConfigForRepoCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposUpdateWebhookConfigForRepoCmd.MarkFlagRequired("owner")
	reposUpdateWebhookConfigForRepoCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposUpdateWebhookConfigForRepoCmd.MarkFlagRequired("repo")
	reposUpdateWebhookConfigForRepoCmd.Flags().String("hook-id", "", "The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.")
	_ = reposUpdateWebhookConfigForRepoCmd.MarkFlagRequired("hook-id")
	reposUpdateWebhookConfigForRepoCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposUpdateWebhookConfigForRepoCmd.Flags().String("url", "", "The URL to which the payloads will be delivered.")
	reposUpdateWebhookConfigForRepoCmd.Flags().String("content-type", "", "The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.")
	reposUpdateWebhookConfigForRepoCmd.Flags().String("secret", "", "If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).")
	reposUpdateWebhookConfigForRepoCmd.Flags().String("insecure-ssl", "", "")
}

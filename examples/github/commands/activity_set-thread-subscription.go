// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var activitySetThreadSubscriptionCmd = &cobra.Command{
	Use:   "set-thread-subscription",
	Short: "Set a thread subscription",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/notifications/threads/{thread_id}/subscription"
		threadIdVal, _ := cmd.Flags().GetString("thread-id")
		if threadIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "thread-id")
		}
		path = strings.Replace(path, "{thread_id}", threadIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "ignored", JSONPath: "ignored", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PUT", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	activitySetThreadSubscriptionCmd.Flags().String("thread-id", "", "The unique identifier of the notification thread. This corresponds to the value returned in the `id` field when you retrieve notifications (for example with the [`GET /notifications` operation](https://docs.github.com/rest/activity/notifications#list-notifications-for-the-authenticated-user)).")
	_ = activitySetThreadSubscriptionCmd.MarkFlagRequired("thread-id")
	activitySetThreadSubscriptionCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	activitySetThreadSubscriptionCmd.Flags().String("ignored", "false", "Whether to block all notifications from a thread.")
}

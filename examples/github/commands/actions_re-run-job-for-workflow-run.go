// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var actionsReRunJobForWorkflowRunCmd = &cobra.Command{
	Use:   "re-run-job-for-workflow-run",
	Short: "Re-run a job from a workflow run",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/actions/jobs/{job_id}/rerun"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		jobIdVal, _ := cmd.Flags().GetString("job-id")
		if jobIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "job-id")
		}
		path = strings.Replace(path, "{job_id}", jobIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "enable-debug-logging", JSONPath: "enable_debug_logging", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	actionsReRunJobForWorkflowRunCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = actionsReRunJobForWorkflowRunCmd.MarkFlagRequired("owner")
	actionsReRunJobForWorkflowRunCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = actionsReRunJobForWorkflowRunCmd.MarkFlagRequired("repo")
	actionsReRunJobForWorkflowRunCmd.Flags().String("job-id", "", "The unique identifier of the job.")
	_ = actionsReRunJobForWorkflowRunCmd.MarkFlagRequired("job-id")
	actionsReRunJobForWorkflowRunCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	actionsReRunJobForWorkflowRunCmd.Flags().String("enable-debug-logging", "false", "Whether to enable debug logging for the re-run.")
}

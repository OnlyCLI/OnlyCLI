// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var orgsSetMembershipForUserCmd = &cobra.Command{
	Use:   "set-membership-for-user",
	Short: "Set organization membership for a user",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/orgs/{org}/memberships/{username}"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)
		usernameVal, _ := cmd.Flags().GetString("username")
		if usernameVal == "" {
			return fmt.Errorf("required flag --%s not set", "username")
		}
		path = strings.Replace(path, "{username}", usernameVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "role", JSONPath: "role", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PUT", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	orgsSetMembershipForUserCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = orgsSetMembershipForUserCmd.MarkFlagRequired("org")
	orgsSetMembershipForUserCmd.Flags().String("username", "", "The handle for the GitHub user account.")
	_ = orgsSetMembershipForUserCmd.MarkFlagRequired("username")
	orgsSetMembershipForUserCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	orgsSetMembershipForUserCmd.Flags().String("role", "member", "The role to give the user in the organization. Can be one of: * `admin` - The user will become an owner of the organization. * `member` - The user will become a non-owner member of the organization.")
	_ = orgsSetMembershipForUserCmd.RegisterFlagCompletionFunc("role", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"admin", "member"}, cobra.ShellCompDirectiveNoFileComp
	})
}

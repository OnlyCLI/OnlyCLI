// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateDeploymentProtectionRuleCmd = &cobra.Command{
	Use:   "create-deployment-protection-rule",
	Short: "Create a custom deployment protection rule on an environment",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules"
		environmentNameVal, _ := cmd.Flags().GetString("environment-name")
		if environmentNameVal == "" {
			return fmt.Errorf("required flag --%s not set", "environment-name")
		}
		path = strings.Replace(path, "{environment_name}", environmentNameVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "integration-id", JSONPath: "integration_id", Type: "integer"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateDeploymentProtectionRuleCmd.Flags().String("environment-name", "", "The name of the environment. The name must be URL encoded. For example, any slashes in the name must be replaced with `%2F`.")
	_ = reposCreateDeploymentProtectionRuleCmd.MarkFlagRequired("environment-name")
	reposCreateDeploymentProtectionRuleCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateDeploymentProtectionRuleCmd.MarkFlagRequired("repo")
	reposCreateDeploymentProtectionRuleCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreateDeploymentProtectionRuleCmd.MarkFlagRequired("owner")
	reposCreateDeploymentProtectionRuleCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateDeploymentProtectionRuleCmd.Flags().String("integration-id", "", "The ID of the custom app that will be enabled on the environment.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposRedeliverWebhookDeliveryCmd = &cobra.Command{
	Use:   "redeliver-webhook-delivery",
	Short: "Redeliver a delivery for a repository webhook",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}/attempts"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		hookIdVal, _ := cmd.Flags().GetString("hook-id")
		if hookIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "hook-id")
		}
		path = strings.Replace(path, "{hook_id}", hookIdVal, 1)
		deliveryIdVal, _ := cmd.Flags().GetString("delivery-id")
		if deliveryIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "delivery-id")
		}
		path = strings.Replace(path, "{delivery_id}", deliveryIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("POST", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposRedeliverWebhookDeliveryCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposRedeliverWebhookDeliveryCmd.MarkFlagRequired("owner")
	reposRedeliverWebhookDeliveryCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposRedeliverWebhookDeliveryCmd.MarkFlagRequired("repo")
	reposRedeliverWebhookDeliveryCmd.Flags().String("hook-id", "", "The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.")
	_ = reposRedeliverWebhookDeliveryCmd.MarkFlagRequired("hook-id")
	reposRedeliverWebhookDeliveryCmd.Flags().String("delivery-id", "", "")
	_ = reposRedeliverWebhookDeliveryCmd.MarkFlagRequired("delivery-id")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var checksCreateCmd = &cobra.Command{
	Use:   "create",
	Short: "Create a check run",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/check-runs"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "head-sha", JSONPath: "head_sha", Type: "string"},
			{FlagName: "details-url", JSONPath: "details_url", Type: "string"},
			{FlagName: "external-id", JSONPath: "external_id", Type: "string"},
			{FlagName: "status", JSONPath: "status", Type: "string"},
			{FlagName: "started-at", JSONPath: "started_at", Type: "string"},
			{FlagName: "conclusion", JSONPath: "conclusion", Type: "string"},
			{FlagName: "completed-at", JSONPath: "completed_at", Type: "string"},
			{FlagName: "output.title", JSONPath: "output.title", Type: "string"},
			{FlagName: "output.summary", JSONPath: "output.summary", Type: "string"},
			{FlagName: "output.text", JSONPath: "output.text", Type: "string"},
			{FlagName: "output.annotations", JSONPath: "output.annotations", Type: "array"},
			{FlagName: "output.images", JSONPath: "output.images", Type: "array"},
			{FlagName: "actions", JSONPath: "actions", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	checksCreateCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = checksCreateCmd.MarkFlagRequired("owner")
	checksCreateCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = checksCreateCmd.MarkFlagRequired("repo")
	checksCreateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	checksCreateCmd.Flags().String("name", "", "The name of the check. For example, \"code-coverage\".")
	_ = checksCreateCmd.MarkFlagRequired("name")
	checksCreateCmd.Flags().String("head-sha", "", "The SHA of the commit.")
	_ = checksCreateCmd.MarkFlagRequired("head-sha")
	checksCreateCmd.Flags().String("details-url", "", "The URL of the integrator's site that has the full details of the check. If the integrator does not provide this, then the homepage of the GitHub app is used.")
	checksCreateCmd.Flags().String("external-id", "", "A reference for the run on the integrator's system.")
	checksCreateCmd.Flags().String("status", "queued", "The current status of the check run. Only GitHub Actions can set a status of `waiting`, `pending`, or `requested`.")
	_ = checksCreateCmd.RegisterFlagCompletionFunc("status", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"queued", "in_progress", "completed", "waiting", "requested", "pending"}, cobra.ShellCompDirectiveNoFileComp
	})
	checksCreateCmd.Flags().String("started-at", "", "The time that the check run began. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
	checksCreateCmd.Flags().String("conclusion", "", "**Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this.")
	_ = checksCreateCmd.RegisterFlagCompletionFunc("conclusion", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"action_required", "cancelled", "failure", "neutral", "success", "skipped", "stale", "timed_out"}, cobra.ShellCompDirectiveNoFileComp
	})
	checksCreateCmd.Flags().String("completed-at", "", "The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
	checksCreateCmd.Flags().String("output.title", "", "The title of the check run.")
	_ = checksCreateCmd.MarkFlagRequired("output.title")
	checksCreateCmd.Flags().String("output.summary", "", "The summary of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters.")
	_ = checksCreateCmd.MarkFlagRequired("output.summary")
	checksCreateCmd.Flags().String("output.text", "", "The details of the check run. This parameter supports Markdown. **Maximum length**: 65535 characters.")
	checksCreateCmd.Flags().String("output.annotations", "", "Adds information from your analysis to specific lines of code. Annotations are visible on GitHub in the **Checks** and **Files changed** tab of the pull request. The Checks API limits the number of annotations to a maximum of 50 per API request. To create more than 50 annotations, you have to make multiple requests to the [Update a check run](https://docs.github.com/rest/checks/runs#update-a-check-run) endpoint. Each time you update the check run, annotations are appended to the list of annotations that already exist for the check run. GitHub Actions are limited to 10 warning annotations and 10 error annotations per step. For details about how you can view annotations on GitHub, see \"[About status checks](https://docs.github.com/articles/about-status-checks#checks)\".")
	checksCreateCmd.Flags().String("output.images", "", "Adds images to the output displayed in the GitHub pull request UI.")
	checksCreateCmd.Flags().String("actions", "", "Displays a button on GitHub that can be clicked to alert your app to do additional tasks. For example, a code linting app can display a button that automatically fixes detected errors. The button created in this object is displayed after the check run completes. When a user clicks the button, GitHub sends the [`check_run.requested_action` webhook](https://docs.github.com/webhooks/event-payloads/#check_run) to your app. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. To learn more about check runs and requested actions, see \"[Check runs and requested actions](https://docs.github.com/rest/guides/using-the-rest-api-to-interact-with-checks#check-runs-and-requested-actions).\"")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposUpdateStatusCheckProtectionCmd = &cobra.Command{
	Use:   "update-status-check-protection",
	Short: "Update status check protection",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		branchVal, _ := cmd.Flags().GetString("branch")
		if branchVal == "" {
			return fmt.Errorf("required flag --%s not set", "branch")
		}
		path = strings.Replace(path, "{branch}", branchVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "strict", JSONPath: "strict", Type: "boolean"},
			{FlagName: "contexts", JSONPath: "contexts", Type: "array"},
			{FlagName: "checks", JSONPath: "checks", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposUpdateStatusCheckProtectionCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposUpdateStatusCheckProtectionCmd.MarkFlagRequired("owner")
	reposUpdateStatusCheckProtectionCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposUpdateStatusCheckProtectionCmd.MarkFlagRequired("repo")
	reposUpdateStatusCheckProtectionCmd.Flags().String("branch", "", "The name of the branch. Cannot contain wildcard characters. To use wildcard characters in branch names, use [the GraphQL API](https://docs.github.com/graphql).")
	_ = reposUpdateStatusCheckProtectionCmd.MarkFlagRequired("branch")
	reposUpdateStatusCheckProtectionCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposUpdateStatusCheckProtectionCmd.Flags().String("strict", "", "Require branches to be up to date before merging.")
	reposUpdateStatusCheckProtectionCmd.Flags().String("contexts", "", "**Closing down notice**: The list of status checks to require in order to merge into this branch. If any of these checks have recently been set by a particular GitHub App, they will be required to come from that app in future for the branch to merge. Use `checks` instead of `contexts` for more fine-grained control.")
	reposUpdateStatusCheckProtectionCmd.Flags().String("checks", "", "The list of status checks to require in order to merge into this branch.")
}

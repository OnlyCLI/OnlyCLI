// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var migrationsUpdateImportCmd = &cobra.Command{
	Use:   "update-import",
	Short: "Update an import",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/import"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "vcs-username", JSONPath: "vcs_username", Type: "string"},
			{FlagName: "vcs-password", JSONPath: "vcs_password", Type: "string"},
			{FlagName: "vcs", JSONPath: "vcs", Type: "string"},
			{FlagName: "tfvc-project", JSONPath: "tfvc_project", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	migrationsUpdateImportCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = migrationsUpdateImportCmd.MarkFlagRequired("owner")
	migrationsUpdateImportCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = migrationsUpdateImportCmd.MarkFlagRequired("repo")
	migrationsUpdateImportCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	migrationsUpdateImportCmd.Flags().String("vcs-username", "", "The username to provide to the originating repository.")
	migrationsUpdateImportCmd.Flags().String("vcs-password", "", "The password to provide to the originating repository.")
	migrationsUpdateImportCmd.Flags().String("vcs", "", "The type of version control system you are migrating from.")
	_ = migrationsUpdateImportCmd.RegisterFlagCompletionFunc("vcs", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"subversion", "tfvc", "git", "mercurial"}, cobra.ShellCompDirectiveNoFileComp
	})
	migrationsUpdateImportCmd.Flags().String("tfvc-project", "", "For a tfvc import, the name of the project that is being imported.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var gitGetBlobCmd = &cobra.Command{
	Use:   "get-blob",
	Short: "Get a blob",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/git/blobs/{file_sha}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		fileShaVal, _ := cmd.Flags().GetString("file-sha")
		if fileShaVal == "" {
			return fmt.Errorf("required flag --%s not set", "file-sha")
		}
		path = strings.Replace(path, "{file_sha}", fileShaVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	gitGetBlobCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = gitGetBlobCmd.MarkFlagRequired("owner")
	gitGetBlobCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = gitGetBlobCmd.MarkFlagRequired("repo")
	gitGetBlobCmd.Flags().String("file-sha", "", "")
	_ = gitGetBlobCmd.MarkFlagRequired("file-sha")
}

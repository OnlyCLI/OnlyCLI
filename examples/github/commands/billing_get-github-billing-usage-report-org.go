// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var billingGetGithubBillingUsageReportOrgCmd = &cobra.Command{
	Use:   "get-github-billing-usage-report-org",
	Short: "Get billing usage report for an organization",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/organizations/{org}/settings/billing/usage"
		orgVal, _ := cmd.Flags().GetString("org")
		if orgVal == "" {
			return fmt.Errorf("required flag --%s not set", "org")
		}
		path = strings.Replace(path, "{org}", orgVal, 1)

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("year"); v != "" {
			params["year"] = v
		}
		if v, _ := cmd.Flags().GetString("month"); v != "" {
			params["month"] = v
		}
		if v, _ := cmd.Flags().GetString("day"); v != "" {
			params["day"] = v
		}
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	billingGetGithubBillingUsageReportOrgCmd.Flags().String("org", "", "The organization name. The name is not case sensitive.")
	_ = billingGetGithubBillingUsageReportOrgCmd.MarkFlagRequired("org")
	billingGetGithubBillingUsageReportOrgCmd.Flags().String("year", "", "If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.")
	billingGetGithubBillingUsageReportOrgCmd.Flags().String("month", "", "If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. If no year is specified the default `year` is used.")
	billingGetGithubBillingUsageReportOrgCmd.Flags().String("day", "", "If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.")
}

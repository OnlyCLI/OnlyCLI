// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var migrationsStartImportCmd = &cobra.Command{
	Use:   "start-import",
	Short: "Start an import",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/import"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "vcs-url", JSONPath: "vcs_url", Type: "string"},
			{FlagName: "vcs", JSONPath: "vcs", Type: "string"},
			{FlagName: "vcs-username", JSONPath: "vcs_username", Type: "string"},
			{FlagName: "vcs-password", JSONPath: "vcs_password", Type: "string"},
			{FlagName: "tfvc-project", JSONPath: "tfvc_project", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PUT", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	migrationsStartImportCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = migrationsStartImportCmd.MarkFlagRequired("owner")
	migrationsStartImportCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = migrationsStartImportCmd.MarkFlagRequired("repo")
	migrationsStartImportCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	migrationsStartImportCmd.Flags().String("vcs-url", "", "The URL of the originating repository.")
	_ = migrationsStartImportCmd.MarkFlagRequired("vcs-url")
	migrationsStartImportCmd.Flags().String("vcs", "", "The originating VCS type. Without this parameter, the import job will take additional time to detect the VCS type before beginning the import. This detection step will be reflected in the response.")
	_ = migrationsStartImportCmd.RegisterFlagCompletionFunc("vcs", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"subversion", "git", "mercurial", "tfvc"}, cobra.ShellCompDirectiveNoFileComp
	})
	migrationsStartImportCmd.Flags().String("vcs-username", "", "If authentication is required, the username to provide to `vcs_url`.")
	migrationsStartImportCmd.Flags().String("vcs-password", "", "If authentication is required, the password to provide to `vcs_url`.")
	migrationsStartImportCmd.Flags().String("tfvc-project", "", "For a tfvc import, the name of the project that is being imported.")
}

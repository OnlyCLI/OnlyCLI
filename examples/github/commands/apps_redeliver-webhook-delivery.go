// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var appsRedeliverWebhookDeliveryCmd = &cobra.Command{
	Use:   "redeliver-webhook-delivery",
	Short: "Redeliver a delivery for an app webhook",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/app/hook/deliveries/{delivery_id}/attempts"
		deliveryIdVal, _ := cmd.Flags().GetString("delivery-id")
		if deliveryIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "delivery-id")
		}
		path = strings.Replace(path, "{delivery_id}", deliveryIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("POST", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	appsRedeliverWebhookDeliveryCmd.Flags().String("delivery-id", "", "")
	_ = appsRedeliverWebhookDeliveryCmd.MarkFlagRequired("delivery-id")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var billingGetGithubBillingUsageReportUserCmd = &cobra.Command{
	Use:   "get-github-billing-usage-report-user",
	Short: "Get billing usage report for a user",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/users/{username}/settings/billing/usage"
		usernameVal, _ := cmd.Flags().GetString("username")
		if usernameVal == "" {
			return fmt.Errorf("required flag --%s not set", "username")
		}
		path = strings.Replace(path, "{username}", usernameVal, 1)

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("year"); v != "" {
			params["year"] = v
		}
		if v, _ := cmd.Flags().GetString("month"); v != "" {
			params["month"] = v
		}
		if v, _ := cmd.Flags().GetString("day"); v != "" {
			params["day"] = v
		}
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	billingGetGithubBillingUsageReportUserCmd.Flags().String("username", "", "The handle for the GitHub user account.")
	_ = billingGetGithubBillingUsageReportUserCmd.MarkFlagRequired("username")
	billingGetGithubBillingUsageReportUserCmd.Flags().String("year", "", "If specified, only return results for a single year. The value of `year` is an integer with four digits representing a year. For example, `2025`. Default value is the current year.")
	billingGetGithubBillingUsageReportUserCmd.Flags().String("month", "", "If specified, only return results for a single month. The value of `month` is an integer between `1` and `12`. If no year is specified the default `year` is used.")
	billingGetGithubBillingUsageReportUserCmd.Flags().String("day", "", "If specified, only return results for a single day. The value of `day` is an integer between `1` and `31`. If no `year` or `month` is specified, the default `year` and `month` are used.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposTransferCmd = &cobra.Command{
	Use:   "transfer",
	Short: "Transfer a repository",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/transfer"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "new-owner", JSONPath: "new_owner", Type: "string"},
			{FlagName: "new-name", JSONPath: "new_name", Type: "string"},
			{FlagName: "team-ids", JSONPath: "team_ids", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposTransferCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposTransferCmd.MarkFlagRequired("owner")
	reposTransferCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposTransferCmd.MarkFlagRequired("repo")
	reposTransferCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposTransferCmd.Flags().String("new-owner", "", "The username or organization name the repository will be transferred to.")
	_ = reposTransferCmd.MarkFlagRequired("new-owner")
	reposTransferCmd.Flags().String("new-name", "", "The new name to be given to the repository.")
	reposTransferCmd.Flags().String("team-ids", "", "ID of the team or teams to add to the repository. Teams can only be added to organization-owned repositories.")
}

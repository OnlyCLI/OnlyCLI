// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var enterpriseTeamsUpdateCmd = &cobra.Command{
	Use:   "update",
	Short: "Update an enterprise team",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/enterprises/{enterprise}/teams/{team_slug}"
		enterpriseVal, _ := cmd.Flags().GetString("enterprise")
		if enterpriseVal == "" {
			return fmt.Errorf("required flag --%s not set", "enterprise")
		}
		path = strings.Replace(path, "{enterprise}", enterpriseVal, 1)
		teamSlugVal, _ := cmd.Flags().GetString("team-slug")
		if teamSlugVal == "" {
			return fmt.Errorf("required flag --%s not set", "team-slug")
		}
		path = strings.Replace(path, "{team_slug}", teamSlugVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "description", JSONPath: "description", Type: "string"},
			{FlagName: "sync-to-organizations", JSONPath: "sync_to_organizations", Type: "string"},
			{FlagName: "organization-selection-type", JSONPath: "organization_selection_type", Type: "string"},
			{FlagName: "group-id", JSONPath: "group_id", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	enterpriseTeamsUpdateCmd.Flags().String("enterprise", "", "The slug version of the enterprise name.")
	_ = enterpriseTeamsUpdateCmd.MarkFlagRequired("enterprise")
	enterpriseTeamsUpdateCmd.Flags().String("team-slug", "", "The slug of the team name.")
	_ = enterpriseTeamsUpdateCmd.MarkFlagRequired("team-slug")
	enterpriseTeamsUpdateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	enterpriseTeamsUpdateCmd.Flags().String("name", "", "A new name for the team.")
	enterpriseTeamsUpdateCmd.Flags().String("description", "", "A new description for the team.")
	enterpriseTeamsUpdateCmd.Flags().String("sync-to-organizations", "disabled", "Retired: this field is no longer supported. Whether the enterprise team should be reflected in each organization. This value cannot be changed.")
	_ = enterpriseTeamsUpdateCmd.RegisterFlagCompletionFunc("sync-to-organizations", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"all", "disabled"}, cobra.ShellCompDirectiveNoFileComp
	})
	enterpriseTeamsUpdateCmd.Flags().String("organization-selection-type", "disabled", "Specifies which organizations in the enterprise should have access to this team. Can be one of `disabled`, `selected`, or `all`. `disabled`: The team is not assigned to any organizations. This is the default when you create a new team. `selected`: The team is assigned to specific organizations. You can then use the [add organization assignments API](https://docs.github.com/rest/enterprise-teams/enterprise-team-organizations#add-organization-assignments). `all`: The team is assigned to all current and future organizations in the enterprise.")
	_ = enterpriseTeamsUpdateCmd.RegisterFlagCompletionFunc("organization-selection-type", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"disabled", "selected", "all"}, cobra.ShellCompDirectiveNoFileComp
	})
	enterpriseTeamsUpdateCmd.Flags().String("group-id", "", "The ID of the IdP group to assign team membership with. The new IdP group will replace the existing one, or replace existing direct members if the team isn't currently linked to an IdP group.")
}

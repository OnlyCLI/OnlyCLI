// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var actionsReRunWorkflowFailedJobsCmd = &cobra.Command{
	Use:   "re-run-workflow-failed-jobs",
	Short: "Re-run failed jobs from a workflow run",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		runIdVal, _ := cmd.Flags().GetString("run-id")
		if runIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "run-id")
		}
		path = strings.Replace(path, "{run_id}", runIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "enable-debug-logging", JSONPath: "enable_debug_logging", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	actionsReRunWorkflowFailedJobsCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = actionsReRunWorkflowFailedJobsCmd.MarkFlagRequired("owner")
	actionsReRunWorkflowFailedJobsCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = actionsReRunWorkflowFailedJobsCmd.MarkFlagRequired("repo")
	actionsReRunWorkflowFailedJobsCmd.Flags().String("run-id", "", "The unique identifier of the workflow run.")
	_ = actionsReRunWorkflowFailedJobsCmd.MarkFlagRequired("run-id")
	actionsReRunWorkflowFailedJobsCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	actionsReRunWorkflowFailedJobsCmd.Flags().String("enable-debug-logging", "false", "Whether to enable debug logging for the re-run.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var secretScanningUpdateAlertCmd = &cobra.Command{
	Use:   "update-alert",
	Short: "Update a secret scanning alert",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		alertNumberVal, _ := cmd.Flags().GetString("alert-number")
		if alertNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "alert-number")
		}
		path = strings.Replace(path, "{alert_number}", alertNumberVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "state", JSONPath: "state", Type: "string"},
			{FlagName: "resolution", JSONPath: "resolution", Type: "string"},
			{FlagName: "resolution-comment", JSONPath: "resolution_comment", Type: "string"},
			{FlagName: "assignee", JSONPath: "assignee", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	secretScanningUpdateAlertCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = secretScanningUpdateAlertCmd.MarkFlagRequired("owner")
	secretScanningUpdateAlertCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = secretScanningUpdateAlertCmd.MarkFlagRequired("repo")
	secretScanningUpdateAlertCmd.Flags().String("alert-number", "", "The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation.")
	_ = secretScanningUpdateAlertCmd.MarkFlagRequired("alert-number")
	secretScanningUpdateAlertCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	secretScanningUpdateAlertCmd.Flags().String("state", "", "Sets the state of the secret scanning alert. You must provide `resolution` when you set the state to `resolved`.")
	_ = secretScanningUpdateAlertCmd.RegisterFlagCompletionFunc("state", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"open", "resolved"}, cobra.ShellCompDirectiveNoFileComp
	})
	secretScanningUpdateAlertCmd.Flags().String("resolution", "", "**Required when the `state` is `resolved`.** The reason for resolving the alert.")
	_ = secretScanningUpdateAlertCmd.RegisterFlagCompletionFunc("resolution", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"false_positive", "wont_fix", "revoked", "used_in_tests"}, cobra.ShellCompDirectiveNoFileComp
	})
	secretScanningUpdateAlertCmd.Flags().String("resolution-comment", "", "An optional comment when closing or reopening an alert. Cannot be updated or deleted.")
	secretScanningUpdateAlertCmd.Flags().String("assignee", "", "The username of the user to assign to the alert. Set to `null` to unassign the alert.")
}

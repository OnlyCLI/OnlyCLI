// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var dependabotUpdateAlertCmd = &cobra.Command{
	Use:   "update-alert",
	Short: "Update a Dependabot alert",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/dependabot/alerts/{alert_number}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		alertNumberVal, _ := cmd.Flags().GetString("alert-number")
		if alertNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "alert-number")
		}
		path = strings.Replace(path, "{alert_number}", alertNumberVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "state", JSONPath: "state", Type: "string"},
			{FlagName: "dismissed-reason", JSONPath: "dismissed_reason", Type: "string"},
			{FlagName: "dismissed-comment", JSONPath: "dismissed_comment", Type: "string"},
			{FlagName: "assignees", JSONPath: "assignees", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	dependabotUpdateAlertCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = dependabotUpdateAlertCmd.MarkFlagRequired("owner")
	dependabotUpdateAlertCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = dependabotUpdateAlertCmd.MarkFlagRequired("repo")
	dependabotUpdateAlertCmd.Flags().String("alert-number", "", "The number that identifies a Dependabot alert in its repository. You can find this at the end of the URL for a Dependabot alert within GitHub, or in `number` fields in the response from the `GET /repos/{owner}/{repo}/dependabot/alerts` operation.")
	_ = dependabotUpdateAlertCmd.MarkFlagRequired("alert-number")
	dependabotUpdateAlertCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	dependabotUpdateAlertCmd.Flags().String("state", "", "The state of the Dependabot alert. A `dismissed_reason` must be provided when setting the state to `dismissed`.")
	_ = dependabotUpdateAlertCmd.RegisterFlagCompletionFunc("state", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"dismissed", "open"}, cobra.ShellCompDirectiveNoFileComp
	})
	dependabotUpdateAlertCmd.Flags().String("dismissed-reason", "", "**Required when `state` is `dismissed`.** A reason for dismissing the alert.")
	_ = dependabotUpdateAlertCmd.RegisterFlagCompletionFunc("dismissed-reason", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"fix_started", "inaccurate", "no_bandwidth", "not_used", "tolerable_risk"}, cobra.ShellCompDirectiveNoFileComp
	})
	dependabotUpdateAlertCmd.Flags().String("dismissed-comment", "", "An optional comment associated with dismissing the alert.")
	dependabotUpdateAlertCmd.Flags().String("assignees", "", "Usernames to assign to this Dependabot Alert. Pass one or more user logins to _replace_ the set of assignees on this alert. Send an empty array (`[]`) to clear all assignees from the alert.")
}

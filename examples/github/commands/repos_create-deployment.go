// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateDeploymentCmd = &cobra.Command{
	Use:   "create-deployment",
	Short: "Create a deployment",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/deployments"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "ref", JSONPath: "ref", Type: "string"},
			{FlagName: "task", JSONPath: "task", Type: "string"},
			{FlagName: "auto-merge", JSONPath: "auto_merge", Type: "boolean"},
			{FlagName: "required-contexts", JSONPath: "required_contexts", Type: "array"},
			{FlagName: "payload", JSONPath: "payload", Type: "string"},
			{FlagName: "environment", JSONPath: "environment", Type: "string"},
			{FlagName: "description", JSONPath: "description", Type: "string"},
			{FlagName: "transient-environment", JSONPath: "transient_environment", Type: "boolean"},
			{FlagName: "production-environment", JSONPath: "production_environment", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateDeploymentCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreateDeploymentCmd.MarkFlagRequired("owner")
	reposCreateDeploymentCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateDeploymentCmd.MarkFlagRequired("repo")
	reposCreateDeploymentCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateDeploymentCmd.Flags().String("ref", "", "The ref to deploy. This can be a branch, tag, or SHA.")
	_ = reposCreateDeploymentCmd.MarkFlagRequired("ref")
	reposCreateDeploymentCmd.Flags().String("task", "deploy", "Specifies a task to execute (e.g., `deploy` or `deploy:migrations`).")
	reposCreateDeploymentCmd.Flags().String("auto-merge", "true", "Attempts to automatically merge the default branch into the requested ref, if it's behind the default branch.")
	reposCreateDeploymentCmd.Flags().String("required-contexts", "", "The [status](https://docs.github.com/rest/commits/statuses) contexts to verify against commit status checks. If you omit this parameter, GitHub verifies all unique contexts before creating a deployment. To bypass checking entirely, pass an empty array. Defaults to all unique contexts.")
	reposCreateDeploymentCmd.Flags().String("payload", "", "")
	reposCreateDeploymentCmd.Flags().String("environment", "production", "Name for the target deployment environment (e.g., `production`, `staging`, `qa`).")
	reposCreateDeploymentCmd.Flags().String("description", "", "Short description of the deployment.")
	reposCreateDeploymentCmd.Flags().String("transient-environment", "false", "Specifies if the given environment is specific to the deployment and will no longer exist at some point in the future. Default: `false`")
	reposCreateDeploymentCmd.Flags().String("production-environment", "", "Specifies if the given environment is one that end-users directly interact with. Default: `true` when `environment` is `production` and `false` otherwise.")
}

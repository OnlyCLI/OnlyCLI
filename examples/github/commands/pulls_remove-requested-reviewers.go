// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var pullsRemoveRequestedReviewersCmd = &cobra.Command{
	Use:   "remove-requested-reviewers",
	Short: "Remove requested reviewers from a pull request",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		pullNumberVal, _ := cmd.Flags().GetString("pull-number")
		if pullNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "pull-number")
		}
		path = strings.Replace(path, "{pull_number}", pullNumberVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "reviewers", JSONPath: "reviewers", Type: "array"},
			{FlagName: "team-reviewers", JSONPath: "team_reviewers", Type: "array"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("DELETE", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	pullsRemoveRequestedReviewersCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = pullsRemoveRequestedReviewersCmd.MarkFlagRequired("owner")
	pullsRemoveRequestedReviewersCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = pullsRemoveRequestedReviewersCmd.MarkFlagRequired("repo")
	pullsRemoveRequestedReviewersCmd.Flags().String("pull-number", "", "The number that identifies the pull request.")
	_ = pullsRemoveRequestedReviewersCmd.MarkFlagRequired("pull-number")
	pullsRemoveRequestedReviewersCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	pullsRemoveRequestedReviewersCmd.Flags().String("reviewers", "", "An array of user `login`s that will be removed.")
	_ = pullsRemoveRequestedReviewersCmd.MarkFlagRequired("reviewers")
	pullsRemoveRequestedReviewersCmd.Flags().String("team-reviewers", "", "An array of team `slug`s that will be removed.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var issuesCreateLabelCmd = &cobra.Command{
	Use:   "create-label",
	Short: "Create a label",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/labels"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "color", JSONPath: "color", Type: "string"},
			{FlagName: "description", JSONPath: "description", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	issuesCreateLabelCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = issuesCreateLabelCmd.MarkFlagRequired("owner")
	issuesCreateLabelCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = issuesCreateLabelCmd.MarkFlagRequired("repo")
	issuesCreateLabelCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	issuesCreateLabelCmd.Flags().String("name", "", "The name of the label. Emoji can be added to label names, using either native emoji or colon-style markup. For example, typing `:strawberry:` will render the emoji ![:strawberry:](https://github.githubassets.com/images/icons/emoji/unicode/1f353.png \":strawberry:\"). For a full list of available emoji and codes, see \"[Emoji cheat sheet](https://github.com/ikatyang/emoji-cheat-sheet).\"")
	_ = issuesCreateLabelCmd.MarkFlagRequired("name")
	issuesCreateLabelCmd.Flags().String("color", "", "The [hexadecimal color code](http://www.color-hex.com/) for the label, without the leading `#`.")
	issuesCreateLabelCmd.Flags().String("description", "", "A short description of the label. Must be 100 characters or fewer.")
}

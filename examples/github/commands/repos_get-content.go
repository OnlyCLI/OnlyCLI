// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposGetContentCmd = &cobra.Command{
	Use:   "get-content",
	Short: "Get repository content",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/contents/{path}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		pathVal, _ := cmd.Flags().GetString("path")
		if pathVal == "" {
			return fmt.Errorf("required flag --%s not set", "path")
		}
		path = strings.Replace(path, "{path}", pathVal, 1)

		params := make(map[string]string)
		if v, _ := cmd.Flags().GetString("ref"); v != "" {
			params["ref"] = v
		}
		bodyFieldDefs := []runtime.BodyFieldDef{}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("GET", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposGetContentCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposGetContentCmd.MarkFlagRequired("owner")
	reposGetContentCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposGetContentCmd.MarkFlagRequired("repo")
	reposGetContentCmd.Flags().String("path", "", "path parameter")
	_ = reposGetContentCmd.MarkFlagRequired("path")
	reposGetContentCmd.Flags().String("ref", "", "The name of the commit/branch/tag. Default: the repository’s default branch.")
	reposGetContentCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposUpdateWebhookCmd = &cobra.Command{
	Use:   "update-webhook",
	Short: "Update a repository webhook",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/hooks/{hook_id}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		hookIdVal, _ := cmd.Flags().GetString("hook-id")
		if hookIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "hook-id")
		}
		path = strings.Replace(path, "{hook_id}", hookIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "config.url", JSONPath: "config.url", Type: "string"},
			{FlagName: "config.content-type", JSONPath: "config.content_type", Type: "string"},
			{FlagName: "config.secret", JSONPath: "config.secret", Type: "string"},
			{FlagName: "config.insecure-ssl", JSONPath: "config.insecure_ssl", Type: "string"},
			{FlagName: "events", JSONPath: "events", Type: "array"},
			{FlagName: "add-events", JSONPath: "add_events", Type: "array"},
			{FlagName: "remove-events", JSONPath: "remove_events", Type: "array"},
			{FlagName: "active", JSONPath: "active", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposUpdateWebhookCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposUpdateWebhookCmd.MarkFlagRequired("owner")
	reposUpdateWebhookCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposUpdateWebhookCmd.MarkFlagRequired("repo")
	reposUpdateWebhookCmd.Flags().String("hook-id", "", "The unique identifier of the hook. You can find this value in the `X-GitHub-Hook-ID` header of a webhook delivery.")
	_ = reposUpdateWebhookCmd.MarkFlagRequired("hook-id")
	reposUpdateWebhookCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposUpdateWebhookCmd.Flags().String("config.url", "", "The URL to which the payloads will be delivered.")
	reposUpdateWebhookCmd.Flags().String("config.content-type", "", "The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.")
	reposUpdateWebhookCmd.Flags().String("config.secret", "", "If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).")
	reposUpdateWebhookCmd.Flags().String("config.insecure-ssl", "", "")
	reposUpdateWebhookCmd.Flags().String("events", "", "Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for. This replaces the entire array of events.")
	reposUpdateWebhookCmd.Flags().String("add-events", "", "Determines a list of events to be added to the list of events that the Hook triggers for.")
	reposUpdateWebhookCmd.Flags().String("remove-events", "", "Determines a list of events to be removed from the list of events that the Hook triggers for.")
	reposUpdateWebhookCmd.Flags().String("active", "true", "Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.")
}

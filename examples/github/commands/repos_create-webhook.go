// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateWebhookCmd = &cobra.Command{
	Use:   "create-webhook",
	Short: "Create a repository webhook",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/hooks"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "name", JSONPath: "name", Type: "string"},
			{FlagName: "config.url", JSONPath: "config.url", Type: "string"},
			{FlagName: "config.content-type", JSONPath: "config.content_type", Type: "string"},
			{FlagName: "config.secret", JSONPath: "config.secret", Type: "string"},
			{FlagName: "config.insecure-ssl", JSONPath: "config.insecure_ssl", Type: "string"},
			{FlagName: "events", JSONPath: "events", Type: "array"},
			{FlagName: "active", JSONPath: "active", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateWebhookCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreateWebhookCmd.MarkFlagRequired("owner")
	reposCreateWebhookCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateWebhookCmd.MarkFlagRequired("repo")
	reposCreateWebhookCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateWebhookCmd.Flags().String("name", "", "Use `web` to create a webhook. Default: `web`. This parameter only accepts the value `web`.")
	reposCreateWebhookCmd.Flags().String("config.url", "", "The URL to which the payloads will be delivered.")
	reposCreateWebhookCmd.Flags().String("config.content-type", "", "The media type used to serialize the payloads. Supported values include `json` and `form`. The default is `form`.")
	reposCreateWebhookCmd.Flags().String("config.secret", "", "If provided, the `secret` will be used as the `key` to generate the HMAC hex digest value for [delivery signature headers](https://docs.github.com/webhooks/event-payloads/#delivery-headers).")
	reposCreateWebhookCmd.Flags().String("config.insecure-ssl", "", "")
	reposCreateWebhookCmd.Flags().String("events", "", "Determines what [events](https://docs.github.com/webhooks/event-payloads) the hook is triggered for.")
	reposCreateWebhookCmd.Flags().String("active", "true", "Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var codeScanningCreateAutofixCmd = &cobra.Command{
	Use:   "create-autofix",
	Short: "Create an autofix for a code scanning alert",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		alertNumberVal, _ := cmd.Flags().GetString("alert-number")
		if alertNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "alert-number")
		}
		path = strings.Replace(path, "{alert_number}", alertNumberVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("POST", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	codeScanningCreateAutofixCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = codeScanningCreateAutofixCmd.MarkFlagRequired("owner")
	codeScanningCreateAutofixCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = codeScanningCreateAutofixCmd.MarkFlagRequired("repo")
	codeScanningCreateAutofixCmd.Flags().String("alert-number", "", "The number that identifies an alert. You can find this at the end of the URL for a code scanning alert within GitHub, and in the `number` field in the response from the `GET /repos/{owner}/{repo}/code-scanning/alerts` operation.")
	_ = codeScanningCreateAutofixCmd.MarkFlagRequired("alert-number")
}

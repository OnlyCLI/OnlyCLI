// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var markdownRenderCmd = &cobra.Command{
	Use:   "render",
	Short: "Render a Markdown document",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/markdown"

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "text", JSONPath: "text", Type: "string"},
			{FlagName: "mode", JSONPath: "mode", Type: "string"},
			{FlagName: "context", JSONPath: "context", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	markdownRenderCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	markdownRenderCmd.Flags().String("text", "", "The Markdown text to render in HTML.")
	_ = markdownRenderCmd.MarkFlagRequired("text")
	markdownRenderCmd.Flags().String("mode", "markdown", "The rendering mode.")
	_ = markdownRenderCmd.RegisterFlagCompletionFunc("mode", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"markdown", "gfm"}, cobra.ShellCompDirectiveNoFileComp
	})
	markdownRenderCmd.Flags().String("context", "", "The repository context to use when creating references in `gfm` mode. For example, setting `context` to `octo-org/octo-repo` will change the text `#42` into an HTML link to issue 42 in the `octo-org/octo-repo` repository.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var actionsSetWorkflowAccessToRepositoryCmd = &cobra.Command{
	Use:   "set-workflow-access-to-repository",
	Short: "Set the level of access for workflows outside of the repository",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/actions/permissions/access"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "access-level", JSONPath: "access_level", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PUT", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	actionsSetWorkflowAccessToRepositoryCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = actionsSetWorkflowAccessToRepositoryCmd.MarkFlagRequired("owner")
	actionsSetWorkflowAccessToRepositoryCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = actionsSetWorkflowAccessToRepositoryCmd.MarkFlagRequired("repo")
	actionsSetWorkflowAccessToRepositoryCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	actionsSetWorkflowAccessToRepositoryCmd.Flags().String("access-level", "", "Defines the level of access that workflows outside of the repository have to actions and reusable workflows within the repository. `none` means the access is only possible from workflows in this repository. `user` level access allows sharing across user owned private repositories only. `organization` level access allows sharing across the organization.")
	_ = actionsSetWorkflowAccessToRepositoryCmd.RegisterFlagCompletionFunc("access-level", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"none", "user", "organization"}, cobra.ShellCompDirectiveNoFileComp
	})
	_ = actionsSetWorkflowAccessToRepositoryCmd.MarkFlagRequired("access-level")
}

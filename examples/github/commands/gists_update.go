// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var gistsUpdateCmd = &cobra.Command{
	Use:   "update",
	Short: "Update a gist",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/gists/{gist_id}"
		gistIdVal, _ := cmd.Flags().GetString("gist-id")
		if gistIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "gist-id")
		}
		path = strings.Replace(path, "{gist_id}", gistIdVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "description", JSONPath: "description", Type: "string"},
			{FlagName: "files", JSONPath: "files", Type: "object"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PATCH", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	gistsUpdateCmd.Flags().String("gist-id", "", "The unique identifier of the gist.")
	_ = gistsUpdateCmd.MarkFlagRequired("gist-id")
	gistsUpdateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	gistsUpdateCmd.Flags().String("description", "", "The description of the gist.")
	gistsUpdateCmd.Flags().String("files", "", "The gist files to be updated, renamed, or deleted. Each `key` must match the current filename (including extension) of the targeted gist file. For example: `hello.py`. To delete a file, set the whole file to null. For example: `hello.py : null`. The file will also be deleted if the specified object does not contain at least one of `content` or `filename`.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var issuesCreateCmd = &cobra.Command{
	Use:   "create",
	Short: "Create an issue",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/issues"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "title", JSONPath: "title", Type: "string"},
			{FlagName: "body", JSONPath: "body", Type: "string"},
			{FlagName: "assignee", JSONPath: "assignee", Type: "string"},
			{FlagName: "milestone", JSONPath: "milestone", Type: "string"},
			{FlagName: "labels", JSONPath: "labels", Type: "array"},
			{FlagName: "assignees", JSONPath: "assignees", Type: "array"},
			{FlagName: "type", JSONPath: "type", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	issuesCreateCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = issuesCreateCmd.MarkFlagRequired("owner")
	issuesCreateCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = issuesCreateCmd.MarkFlagRequired("repo")
	issuesCreateCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	issuesCreateCmd.Flags().String("title", "", "The title of the issue.")
	_ = issuesCreateCmd.MarkFlagRequired("title")
	issuesCreateCmd.Flags().String("body", "", "The contents of the issue.")
	issuesCreateCmd.Flags().String("assignee", "", "Login for the user that this issue should be assigned to. _NOTE: Only users with push access can set the assignee for new issues. The assignee is silently dropped otherwise. **This field is closing down.**_")
	issuesCreateCmd.Flags().String("milestone", "", "")
	issuesCreateCmd.Flags().String("labels", "", "Labels to associate with this issue. _NOTE: Only users with push access can set labels for new issues. Labels are silently dropped otherwise._")
	issuesCreateCmd.Flags().String("assignees", "", "Logins for Users to assign to this issue. _NOTE: Only users with push access can set assignees for new issues. Assignees are silently dropped otherwise._")
	issuesCreateCmd.Flags().String("type", "", "The name of the issue type to associate with this issue. _NOTE: Only users with push access can set the type for new issues. The type is silently dropped otherwise._")
}

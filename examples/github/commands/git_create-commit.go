// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var gitCreateCommitCmd = &cobra.Command{
	Use:   "create-commit",
	Short: "Create a commit",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/git/commits"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "message", JSONPath: "message", Type: "string"},
			{FlagName: "tree", JSONPath: "tree", Type: "string"},
			{FlagName: "parents", JSONPath: "parents", Type: "array"},
			{FlagName: "author.name", JSONPath: "author.name", Type: "string"},
			{FlagName: "author.email", JSONPath: "author.email", Type: "string"},
			{FlagName: "author.date", JSONPath: "author.date", Type: "string"},
			{FlagName: "committer.name", JSONPath: "committer.name", Type: "string"},
			{FlagName: "committer.email", JSONPath: "committer.email", Type: "string"},
			{FlagName: "committer.date", JSONPath: "committer.date", Type: "string"},
			{FlagName: "signature", JSONPath: "signature", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	gitCreateCommitCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = gitCreateCommitCmd.MarkFlagRequired("owner")
	gitCreateCommitCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = gitCreateCommitCmd.MarkFlagRequired("repo")
	gitCreateCommitCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	gitCreateCommitCmd.Flags().String("message", "", "The commit message")
	_ = gitCreateCommitCmd.MarkFlagRequired("message")
	gitCreateCommitCmd.Flags().String("tree", "", "The SHA of the tree object this commit points to")
	_ = gitCreateCommitCmd.MarkFlagRequired("tree")
	gitCreateCommitCmd.Flags().String("parents", "", "The full SHAs of the commits that were the parents of this commit. If omitted or empty, the commit will be written as a root commit. For a single parent, an array of one SHA should be provided; for a merge commit, an array of more than one should be provided.")
	gitCreateCommitCmd.Flags().String("author.name", "", "The name of the author (or committer) of the commit")
	_ = gitCreateCommitCmd.MarkFlagRequired("author.name")
	gitCreateCommitCmd.Flags().String("author.email", "", "The email of the author (or committer) of the commit")
	_ = gitCreateCommitCmd.MarkFlagRequired("author.email")
	gitCreateCommitCmd.Flags().String("author.date", "", "Indicates when this commit was authored (or committed). This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
	gitCreateCommitCmd.Flags().String("committer.name", "", "The name of the author (or committer) of the commit")
	gitCreateCommitCmd.Flags().String("committer.email", "", "The email of the author (or committer) of the commit")
	gitCreateCommitCmd.Flags().String("committer.date", "", "Indicates when this commit was authored (or committed). This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
	gitCreateCommitCmd.Flags().String("signature", "", "The [PGP signature](https://en.wikipedia.org/wiki/Pretty_Good_Privacy) of the commit. GitHub adds the signature to the `gpgsig` header of the created commit. For a commit signature to be verifiable by Git or GitHub, it must be an ASCII-armored detached PGP signature over the string commit as it would be written to the object database. To pass a `signature` parameter, you need to first manually create a valid PGP signature, which can be complicated. You may find it easier to [use the command line](https://git-scm.com/book/id/v2/Git-Tools-Signing-Your-Work) to create signed commits.")
}

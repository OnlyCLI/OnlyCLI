// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var pullsMergeCmd = &cobra.Command{
	Use:   "merge",
	Short: "Merge a pull request",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/pulls/{pull_number}/merge"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		pullNumberVal, _ := cmd.Flags().GetString("pull-number")
		if pullNumberVal == "" {
			return fmt.Errorf("required flag --%s not set", "pull-number")
		}
		path = strings.Replace(path, "{pull_number}", pullNumberVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "commit-title", JSONPath: "commit_title", Type: "string"},
			{FlagName: "commit-message", JSONPath: "commit_message", Type: "string"},
			{FlagName: "sha", JSONPath: "sha", Type: "string"},
			{FlagName: "merge-method", JSONPath: "merge_method", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PUT", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	pullsMergeCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = pullsMergeCmd.MarkFlagRequired("owner")
	pullsMergeCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = pullsMergeCmd.MarkFlagRequired("repo")
	pullsMergeCmd.Flags().String("pull-number", "", "The number that identifies the pull request.")
	_ = pullsMergeCmd.MarkFlagRequired("pull-number")
	pullsMergeCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	pullsMergeCmd.Flags().String("commit-title", "", "Title for the automatic commit message.")
	pullsMergeCmd.Flags().String("commit-message", "", "Extra detail to append to automatic commit message.")
	pullsMergeCmd.Flags().String("sha", "", "SHA that pull request head must match to allow merge.")
	pullsMergeCmd.Flags().String("merge-method", "", "The merge method to use.")
	_ = pullsMergeCmd.RegisterFlagCompletionFunc("merge-method", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"merge", "squash", "rebase"}, cobra.ShellCompDirectiveNoFileComp
	})
}

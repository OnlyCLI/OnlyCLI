// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var checksGetSuiteCmd = &cobra.Command{
	Use:   "get-suite",
	Short: "Get a check suite",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/check-suites/{check_suite_id}"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)
		checkSuiteIdVal, _ := cmd.Flags().GetString("check-suite-id")
		if checkSuiteIdVal == "" {
			return fmt.Errorf("required flag --%s not set", "check-suite-id")
		}
		path = strings.Replace(path, "{check_suite_id}", checkSuiteIdVal, 1)

		params := make(map[string]string)
		resp, err := client.Do("GET", path, params, nil)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	checksGetSuiteCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = checksGetSuiteCmd.MarkFlagRequired("owner")
	checksGetSuiteCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = checksGetSuiteCmd.MarkFlagRequired("repo")
	checksGetSuiteCmd.Flags().String("check-suite-id", "", "The unique identifier of the check suite.")
	_ = checksGetSuiteCmd.MarkFlagRequired("check-suite-id")
}

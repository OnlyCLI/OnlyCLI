// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var gitCreateTagCmd = &cobra.Command{
	Use:   "create-tag",
	Short: "Create a tag object",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/git/tags"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "tag", JSONPath: "tag", Type: "string"},
			{FlagName: "message", JSONPath: "message", Type: "string"},
			{FlagName: "object", JSONPath: "object", Type: "string"},
			{FlagName: "type", JSONPath: "type", Type: "string"},
			{FlagName: "tagger.name", JSONPath: "tagger.name", Type: "string"},
			{FlagName: "tagger.email", JSONPath: "tagger.email", Type: "string"},
			{FlagName: "tagger.date", JSONPath: "tagger.date", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	gitCreateTagCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = gitCreateTagCmd.MarkFlagRequired("owner")
	gitCreateTagCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = gitCreateTagCmd.MarkFlagRequired("repo")
	gitCreateTagCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	gitCreateTagCmd.Flags().String("tag", "", "The tag's name. This is typically a version (e.g., \"v0.0.1\").")
	_ = gitCreateTagCmd.MarkFlagRequired("tag")
	gitCreateTagCmd.Flags().String("message", "", "The tag message.")
	_ = gitCreateTagCmd.MarkFlagRequired("message")
	gitCreateTagCmd.Flags().String("object", "", "The SHA of the git object this is tagging.")
	_ = gitCreateTagCmd.MarkFlagRequired("object")
	gitCreateTagCmd.Flags().String("type", "", "The type of the object we're tagging. Normally this is a `commit` but it can also be a `tree` or a `blob`.")
	_ = gitCreateTagCmd.RegisterFlagCompletionFunc("type", func(cmd *cobra.Command, args []string, toComplete string) ([]string, cobra.ShellCompDirective) {
		return []string{"commit", "tree", "blob"}, cobra.ShellCompDirectiveNoFileComp
	})
	_ = gitCreateTagCmd.MarkFlagRequired("type")
	gitCreateTagCmd.Flags().String("tagger.name", "", "The name of the author of the tag")
	_ = gitCreateTagCmd.MarkFlagRequired("tagger.name")
	gitCreateTagCmd.Flags().String("tagger.email", "", "The email of the author of the tag")
	_ = gitCreateTagCmd.MarkFlagRequired("tagger.email")
	gitCreateTagCmd.Flags().String("tagger.date", "", "When this object was tagged. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
}

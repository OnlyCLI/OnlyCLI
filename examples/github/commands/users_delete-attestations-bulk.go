// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var usersDeleteAttestationsBulkCmd = &cobra.Command{
	Use:   "delete-attestations-bulk",
	Short: "Delete attestations in bulk",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/users/{username}/attestations/delete-request"
		usernameVal, _ := cmd.Flags().GetString("username")
		if usernameVal == "" {
			return fmt.Errorf("required flag --%s not set", "username")
		}
		path = strings.Replace(path, "{username}", usernameVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	usersDeleteAttestationsBulkCmd.Flags().String("username", "", "The handle for the GitHub user account.")
	_ = usersDeleteAttestationsBulkCmd.MarkFlagRequired("username")
	usersDeleteAttestationsBulkCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
}

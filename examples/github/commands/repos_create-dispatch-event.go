// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposCreateDispatchEventCmd = &cobra.Command{
	Use:   "create-dispatch-event",
	Short: "Create a repository dispatch event",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/dispatches"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "event-type", JSONPath: "event_type", Type: "string"},
			{FlagName: "client-payload", JSONPath: "client_payload", Type: "object"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposCreateDispatchEventCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposCreateDispatchEventCmd.MarkFlagRequired("owner")
	reposCreateDispatchEventCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposCreateDispatchEventCmd.MarkFlagRequired("repo")
	reposCreateDispatchEventCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposCreateDispatchEventCmd.Flags().String("event-type", "", "A custom webhook event name. Must be 100 characters or fewer.")
	_ = reposCreateDispatchEventCmd.MarkFlagRequired("event-type")
	reposCreateDispatchEventCmd.Flags().String("client-payload", "", "JSON payload with extra information about the webhook event that your action or workflow may use. The maximum number of top-level properties is 10. The total size of the JSON payload must be less than 64KB.")
}

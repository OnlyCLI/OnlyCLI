// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var reposMergeCmd = &cobra.Command{
	Use:   "merge",
	Short: "Merge a branch",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/merges"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "base", JSONPath: "base", Type: "string"},
			{FlagName: "head", JSONPath: "head", Type: "string"},
			{FlagName: "commit-message", JSONPath: "commit_message", Type: "string"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("POST", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	reposMergeCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = reposMergeCmd.MarkFlagRequired("owner")
	reposMergeCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = reposMergeCmd.MarkFlagRequired("repo")
	reposMergeCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	reposMergeCmd.Flags().String("base", "", "The name of the base branch that the head will be merged into.")
	_ = reposMergeCmd.MarkFlagRequired("base")
	reposMergeCmd.Flags().String("head", "", "The head to merge. This can be a branch name or a commit SHA1.")
	_ = reposMergeCmd.MarkFlagRequired("head")
	reposMergeCmd.Flags().String("commit-message", "", "Commit message to use for the merge commit. If omitted, a default message will be used.")
}

// Code generated by onlycli; DO NOT EDIT.

package commands

import (
	"fmt"
	"strings"

	"github.com/github/github-cli/runtime"
	"github.com/spf13/cobra"
)

var activitySetRepoSubscriptionCmd = &cobra.Command{
	Use:   "set-repo-subscription",
	Short: "Set a repository subscription",
	RunE: func(cmd *cobra.Command, args []string) error {
		client := runtime.NewClientFromConfig(cmd)

		path := "/repos/{owner}/{repo}/subscription"
		ownerVal, _ := cmd.Flags().GetString("owner")
		if ownerVal == "" {
			return fmt.Errorf("required flag --%s not set", "owner")
		}
		path = strings.Replace(path, "{owner}", ownerVal, 1)
		repoVal, _ := cmd.Flags().GetString("repo")
		if repoVal == "" {
			return fmt.Errorf("required flag --%s not set", "repo")
		}
		path = strings.Replace(path, "{repo}", repoVal, 1)

		params := make(map[string]string)
		bodyFieldDefs := []runtime.BodyFieldDef{
			{FlagName: "subscribed", JSONPath: "subscribed", Type: "boolean"},
			{FlagName: "ignored", JSONPath: "ignored", Type: "boolean"},
		}
		bodyBytes, err := runtime.BuildBody(cmd, bodyFieldDefs)
		if err != nil {
			return fmt.Errorf("build request body: %w", err)
		}
		resp, err := client.Do("PUT", path, params, bodyBytes)
		if err != nil {
			return err
		}

		return runtime.PrintOutput(cmd, resp)
	},
}

func init() {
	activitySetRepoSubscriptionCmd.Flags().String("owner", "", "The account owner of the repository. The name is not case sensitive.")
	_ = activitySetRepoSubscriptionCmd.MarkFlagRequired("owner")
	activitySetRepoSubscriptionCmd.Flags().String("repo", "", "The name of the repository without the `.git` extension. The name is not case sensitive.")
	_ = activitySetRepoSubscriptionCmd.MarkFlagRequired("repo")
	activitySetRepoSubscriptionCmd.Flags().String("data", "", "Request body JSON, @file, or @- for stdin")
	activitySetRepoSubscriptionCmd.Flags().String("subscribed", "", "Determines if notifications should be received from this repository.")
	activitySetRepoSubscriptionCmd.Flags().String("ignored", "", "Determines if all notifications should be blocked from this repository.")
}
